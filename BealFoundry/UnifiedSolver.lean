import BealFoundry.Governance
import BealFoundry.Lambda
import BealFoundry.Soundness
import BealFoundry.Observer

/-!
# Unified Solver Output

Generated by unified_solver.py
Beal: 455 signatures, 455 clear, 0 with solutions

Domains: Beal (active), NS (stub), Riemann (stub)
-/

-- ═══════════════════════════════════════════════════════════════════
-- BEAL CONJECTURE
-- ═══════════════════════════════════════════════════════════════════

/-!
# Unified Solver: Beal Signature Enumeration

Checked 455 signatures with search bound 1000.
All exponents in [3, 15].

Generated by unified_solver.py — observer output.
Every native_decide is verified by Lean's kernel, not the solver.
-/

namespace BealFoundry.Solver

-- (3,3,3): κ=1.0000 [euclidean] solutions=[] bound=1000 [45.5ms]
/-- (3,3,3): κ = 1.000000. -/
def sig333 : BealFoundry.Signature := ⟨3, 3, 3, by omega, by omega, by omega⟩

theorem sig333_euclidean : sig333.regime = .euclidean := by
  native_decide

/-- Searched a^3 + b^3 = c^3 up to 1000: no coprime solutions. -/
def cert_sig333 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig333_hard : cert_sig333.isHard = true := by native_decide

-- (3,3,4): κ=0.9167 [hyperbolic] solutions=[] bound=1000 [45.0ms]
/-- (3,3,4): κ = 0.916667. -/
def sig334 : BealFoundry.Signature := ⟨3, 3, 4, by omega, by omega, by omega⟩

theorem sig334_hyperbolic : sig334.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^4 up to 1000: no coprime solutions. -/
def cert_sig334 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig334_hard : cert_sig334.isHard = true := by native_decide

-- (3,3,5): κ=0.8667 [hyperbolic] solutions=[] bound=1000 [46.4ms]
/-- (3,3,5): κ = 0.866667. -/
def sig335 : BealFoundry.Signature := ⟨3, 3, 5, by omega, by omega, by omega⟩

theorem sig335_hyperbolic : sig335.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^5 up to 1000: no coprime solutions. -/
def cert_sig335 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig335_hard : cert_sig335.isHard = true := by native_decide

-- (3,3,6): κ=0.8333 [hyperbolic] solutions=[] bound=1000 [44.1ms]
/-- (3,3,6): κ = 0.833333. -/
def sig336 : BealFoundry.Signature := ⟨3, 3, 6, by omega, by omega, by omega⟩

theorem sig336_hyperbolic : sig336.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^6 up to 1000: no coprime solutions. -/
def cert_sig336 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig336_hard : cert_sig336.isHard = true := by native_decide

-- (3,3,7): κ=0.8095 [hyperbolic] solutions=[] bound=1000 [43.8ms]
/-- (3,3,7): κ = 0.809524. -/
def sig337 : BealFoundry.Signature := ⟨3, 3, 7, by omega, by omega, by omega⟩

theorem sig337_hyperbolic : sig337.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^7 up to 1000: no coprime solutions. -/
def cert_sig337 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig337_hard : cert_sig337.isHard = true := by native_decide

-- (3,3,8): κ=0.7917 [hyperbolic] solutions=[] bound=1000 [44.3ms]
/-- (3,3,8): κ = 0.791667. -/
def sig338 : BealFoundry.Signature := ⟨3, 3, 8, by omega, by omega, by omega⟩

theorem sig338_hyperbolic : sig338.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^8 up to 1000: no coprime solutions. -/
def cert_sig338 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig338_hard : cert_sig338.isHard = true := by native_decide

-- (3,3,9): κ=0.7778 [hyperbolic] solutions=[] bound=1000 [44.0ms]
/-- (3,3,9): κ = 0.777778. -/
def sig339 : BealFoundry.Signature := ⟨3, 3, 9, by omega, by omega, by omega⟩

theorem sig339_hyperbolic : sig339.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^9 up to 1000: no coprime solutions. -/
def cert_sig339 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig339_hard : cert_sig339.isHard = true := by native_decide

-- (3,3,10): κ=0.7667 [hyperbolic] solutions=[] bound=1000 [43.7ms]
/-- (3,3,10): κ = 0.766667. -/
def sig3310 : BealFoundry.Signature := ⟨3, 3, 10, by omega, by omega, by omega⟩

theorem sig3310_hyperbolic : sig3310.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^10 up to 1000: no coprime solutions. -/
def cert_sig3310 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3310_hard : cert_sig3310.isHard = true := by native_decide

-- (3,3,11): κ=0.7576 [hyperbolic] solutions=[] bound=1000 [44.1ms]
/-- (3,3,11): κ = 0.757576. -/
def sig3311 : BealFoundry.Signature := ⟨3, 3, 11, by omega, by omega, by omega⟩

theorem sig3311_hyperbolic : sig3311.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^11 up to 1000: no coprime solutions. -/
def cert_sig3311 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3311_hard : cert_sig3311.isHard = true := by native_decide

-- (3,3,12): κ=0.7500 [hyperbolic] solutions=[] bound=1000 [43.8ms]
/-- (3,3,12): κ = 0.750000. -/
def sig3312 : BealFoundry.Signature := ⟨3, 3, 12, by omega, by omega, by omega⟩

theorem sig3312_hyperbolic : sig3312.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^12 up to 1000: no coprime solutions. -/
def cert_sig3312 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3312_hard : cert_sig3312.isHard = true := by native_decide

-- (3,3,13): κ=0.7436 [hyperbolic] solutions=[] bound=1000 [43.5ms]
/-- (3,3,13): κ = 0.743590. -/
def sig3313 : BealFoundry.Signature := ⟨3, 3, 13, by omega, by omega, by omega⟩

theorem sig3313_hyperbolic : sig3313.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^13 up to 1000: no coprime solutions. -/
def cert_sig3313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3313_hard : cert_sig3313.isHard = true := by native_decide

-- (3,3,14): κ=0.7381 [hyperbolic] solutions=[] bound=1000 [46.6ms]
/-- (3,3,14): κ = 0.738095. -/
def sig3314 : BealFoundry.Signature := ⟨3, 3, 14, by omega, by omega, by omega⟩

theorem sig3314_hyperbolic : sig3314.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^14 up to 1000: no coprime solutions. -/
def cert_sig3314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3314_hard : cert_sig3314.isHard = true := by native_decide

-- (3,3,15): κ=0.7333 [hyperbolic] solutions=[] bound=1000 [43.9ms]
/-- (3,3,15): κ = 0.733333. -/
def sig3315 : BealFoundry.Signature := ⟨3, 3, 15, by omega, by omega, by omega⟩

theorem sig3315_hyperbolic : sig3315.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^3 = c^15 up to 1000: no coprime solutions. -/
def cert_sig3315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3315_hard : cert_sig3315.isHard = true := by native_decide

-- (3,4,4): κ=0.8333 [hyperbolic] solutions=[] bound=1000 [46.4ms]
/-- (3,4,4): κ = 0.833333. -/
def sig344 : BealFoundry.Signature := ⟨3, 4, 4, by omega, by omega, by omega⟩

theorem sig344_hyperbolic : sig344.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^4 up to 1000: no coprime solutions. -/
def cert_sig344 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig344_hard : cert_sig344.isHard = true := by native_decide

-- (3,4,5): κ=0.7833 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (3,4,5): κ = 0.783333. -/
def sig345 : BealFoundry.Signature := ⟨3, 4, 5, by omega, by omega, by omega⟩

theorem sig345_hyperbolic : sig345.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^5 up to 1000: no coprime solutions. -/
def cert_sig345 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig345_hard : cert_sig345.isHard = true := by native_decide

-- (3,4,6): κ=0.7500 [hyperbolic] solutions=[] bound=1000 [46.5ms]
/-- (3,4,6): κ = 0.750000. -/
def sig346 : BealFoundry.Signature := ⟨3, 4, 6, by omega, by omega, by omega⟩

theorem sig346_hyperbolic : sig346.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^6 up to 1000: no coprime solutions. -/
def cert_sig346 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig346_hard : cert_sig346.isHard = true := by native_decide

-- (3,4,7): κ=0.7262 [hyperbolic] solutions=[] bound=1000 [46.8ms]
/-- (3,4,7): κ = 0.726190. -/
def sig347 : BealFoundry.Signature := ⟨3, 4, 7, by omega, by omega, by omega⟩

theorem sig347_hyperbolic : sig347.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^7 up to 1000: no coprime solutions. -/
def cert_sig347 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig347_hard : cert_sig347.isHard = true := by native_decide

-- (3,4,8): κ=0.7083 [hyperbolic] solutions=[] bound=1000 [47.1ms]
/-- (3,4,8): κ = 0.708333. -/
def sig348 : BealFoundry.Signature := ⟨3, 4, 8, by omega, by omega, by omega⟩

theorem sig348_hyperbolic : sig348.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^8 up to 1000: no coprime solutions. -/
def cert_sig348 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig348_hard : cert_sig348.isHard = true := by native_decide

-- (3,4,9): κ=0.6944 [hyperbolic] solutions=[] bound=1000 [46.8ms]
/-- (3,4,9): κ = 0.694444. -/
def sig349 : BealFoundry.Signature := ⟨3, 4, 9, by omega, by omega, by omega⟩

theorem sig349_hyperbolic : sig349.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^9 up to 1000: no coprime solutions. -/
def cert_sig349 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig349_hard : cert_sig349.isHard = true := by native_decide

-- (3,4,10): κ=0.6833 [hyperbolic] solutions=[] bound=1000 [46.2ms]
/-- (3,4,10): κ = 0.683333. -/
def sig3410 : BealFoundry.Signature := ⟨3, 4, 10, by omega, by omega, by omega⟩

theorem sig3410_hyperbolic : sig3410.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^10 up to 1000: no coprime solutions. -/
def cert_sig3410 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3410_hard : cert_sig3410.isHard = true := by native_decide

-- (3,4,11): κ=0.6742 [hyperbolic] solutions=[] bound=1000 [46.4ms]
/-- (3,4,11): κ = 0.674242. -/
def sig3411 : BealFoundry.Signature := ⟨3, 4, 11, by omega, by omega, by omega⟩

theorem sig3411_hyperbolic : sig3411.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^11 up to 1000: no coprime solutions. -/
def cert_sig3411 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3411_hard : cert_sig3411.isHard = true := by native_decide

-- (3,4,12): κ=0.6667 [hyperbolic] solutions=[] bound=1000 [46.6ms]
/-- (3,4,12): κ = 0.666667. -/
def sig3412 : BealFoundry.Signature := ⟨3, 4, 12, by omega, by omega, by omega⟩

theorem sig3412_hyperbolic : sig3412.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^12 up to 1000: no coprime solutions. -/
def cert_sig3412 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3412_hard : cert_sig3412.isHard = true := by native_decide

-- (3,4,13): κ=0.6603 [hyperbolic] solutions=[] bound=1000 [46.3ms]
/-- (3,4,13): κ = 0.660256. -/
def sig3413 : BealFoundry.Signature := ⟨3, 4, 13, by omega, by omega, by omega⟩

theorem sig3413_hyperbolic : sig3413.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^13 up to 1000: no coprime solutions. -/
def cert_sig3413 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3413_hard : cert_sig3413.isHard = true := by native_decide

-- (3,4,14): κ=0.6548 [hyperbolic] solutions=[] bound=1000 [46.2ms]
/-- (3,4,14): κ = 0.654762. -/
def sig3414 : BealFoundry.Signature := ⟨3, 4, 14, by omega, by omega, by omega⟩

theorem sig3414_hyperbolic : sig3414.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^14 up to 1000: no coprime solutions. -/
def cert_sig3414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3414_hard : cert_sig3414.isHard = true := by native_decide

-- (3,4,15): κ=0.6500 [hyperbolic] solutions=[] bound=1000 [46.0ms]
/-- (3,4,15): κ = 0.650000. -/
def sig3415 : BealFoundry.Signature := ⟨3, 4, 15, by omega, by omega, by omega⟩

theorem sig3415_hyperbolic : sig3415.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^4 = c^15 up to 1000: no coprime solutions. -/
def cert_sig3415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3415_hard : cert_sig3415.isHard = true := by native_decide

-- (3,5,5): κ=0.7333 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (3,5,5): κ = 0.733333. -/
def sig355 : BealFoundry.Signature := ⟨3, 5, 5, by omega, by omega, by omega⟩

theorem sig355_hyperbolic : sig355.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^5 up to 1000: no coprime solutions. -/
def cert_sig355 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig355_hard : cert_sig355.isHard = true := by native_decide

-- (3,5,6): κ=0.7000 [hyperbolic] solutions=[] bound=1000 [46.3ms]
/-- (3,5,6): κ = 0.700000. -/
def sig356 : BealFoundry.Signature := ⟨3, 5, 6, by omega, by omega, by omega⟩

theorem sig356_hyperbolic : sig356.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^6 up to 1000: no coprime solutions. -/
def cert_sig356 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig356_hard : cert_sig356.isHard = true := by native_decide

-- (3,5,7): κ=0.6762 [hyperbolic] solutions=[] bound=1000 [47.1ms]
/-- (3,5,7): κ = 0.676190. -/
def sig357 : BealFoundry.Signature := ⟨3, 5, 7, by omega, by omega, by omega⟩

theorem sig357_hyperbolic : sig357.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^7 up to 1000: no coprime solutions. -/
def cert_sig357 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig357_hard : cert_sig357.isHard = true := by native_decide

-- (3,5,8): κ=0.6583 [hyperbolic] solutions=[] bound=1000 [47.5ms]
/-- (3,5,8): κ = 0.658333. -/
def sig358 : BealFoundry.Signature := ⟨3, 5, 8, by omega, by omega, by omega⟩

theorem sig358_hyperbolic : sig358.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^8 up to 1000: no coprime solutions. -/
def cert_sig358 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig358_hard : cert_sig358.isHard = true := by native_decide

-- (3,5,9): κ=0.6444 [hyperbolic] solutions=[] bound=1000 [47.7ms]
/-- (3,5,9): κ = 0.644444. -/
def sig359 : BealFoundry.Signature := ⟨3, 5, 9, by omega, by omega, by omega⟩

theorem sig359_hyperbolic : sig359.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^9 up to 1000: no coprime solutions. -/
def cert_sig359 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig359_hard : cert_sig359.isHard = true := by native_decide

-- (3,5,10): κ=0.6333 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (3,5,10): κ = 0.633333. -/
def sig3510 : BealFoundry.Signature := ⟨3, 5, 10, by omega, by omega, by omega⟩

theorem sig3510_hyperbolic : sig3510.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^10 up to 1000: no coprime solutions. -/
def cert_sig3510 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3510_hard : cert_sig3510.isHard = true := by native_decide

-- (3,5,11): κ=0.6242 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (3,5,11): κ = 0.624242. -/
def sig3511 : BealFoundry.Signature := ⟨3, 5, 11, by omega, by omega, by omega⟩

theorem sig3511_hyperbolic : sig3511.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^11 up to 1000: no coprime solutions. -/
def cert_sig3511 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3511_hard : cert_sig3511.isHard = true := by native_decide

-- (3,5,12): κ=0.6167 [hyperbolic] solutions=[] bound=1000 [47.4ms]
/-- (3,5,12): κ = 0.616667. -/
def sig3512 : BealFoundry.Signature := ⟨3, 5, 12, by omega, by omega, by omega⟩

theorem sig3512_hyperbolic : sig3512.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^12 up to 1000: no coprime solutions. -/
def cert_sig3512 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3512_hard : cert_sig3512.isHard = true := by native_decide

-- (3,5,13): κ=0.6103 [hyperbolic] solutions=[] bound=1000 [46.6ms]
/-- (3,5,13): κ = 0.610256. -/
def sig3513 : BealFoundry.Signature := ⟨3, 5, 13, by omega, by omega, by omega⟩

theorem sig3513_hyperbolic : sig3513.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^13 up to 1000: no coprime solutions. -/
def cert_sig3513 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3513_hard : cert_sig3513.isHard = true := by native_decide

-- (3,5,14): κ=0.6048 [hyperbolic] solutions=[] bound=1000 [47.0ms]
/-- (3,5,14): κ = 0.604762. -/
def sig3514 : BealFoundry.Signature := ⟨3, 5, 14, by omega, by omega, by omega⟩

theorem sig3514_hyperbolic : sig3514.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^14 up to 1000: no coprime solutions. -/
def cert_sig3514 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3514_hard : cert_sig3514.isHard = true := by native_decide

-- (3,5,15): κ=0.6000 [hyperbolic] solutions=[] bound=1000 [46.8ms]
/-- (3,5,15): κ = 0.600000. -/
def sig3515 : BealFoundry.Signature := ⟨3, 5, 15, by omega, by omega, by omega⟩

theorem sig3515_hyperbolic : sig3515.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^5 = c^15 up to 1000: no coprime solutions. -/
def cert_sig3515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3515_hard : cert_sig3515.isHard = true := by native_decide

-- (3,6,6): κ=0.6667 [hyperbolic] solutions=[] bound=1000 [45.7ms]
/-- (3,6,6): κ = 0.666667. -/
def sig366 : BealFoundry.Signature := ⟨3, 6, 6, by omega, by omega, by omega⟩

theorem sig366_hyperbolic : sig366.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^6 up to 1000: no coprime solutions. -/
def cert_sig366 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig366_hard : cert_sig366.isHard = true := by native_decide

-- (3,6,7): κ=0.6429 [hyperbolic] solutions=[] bound=1000 [46.7ms]
/-- (3,6,7): κ = 0.642857. -/
def sig367 : BealFoundry.Signature := ⟨3, 6, 7, by omega, by omega, by omega⟩

theorem sig367_hyperbolic : sig367.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^7 up to 1000: no coprime solutions. -/
def cert_sig367 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig367_hard : cert_sig367.isHard = true := by native_decide

-- (3,6,8): κ=0.6250 [hyperbolic] solutions=[] bound=1000 [46.5ms]
/-- (3,6,8): κ = 0.625000. -/
def sig368 : BealFoundry.Signature := ⟨3, 6, 8, by omega, by omega, by omega⟩

theorem sig368_hyperbolic : sig368.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^8 up to 1000: no coprime solutions. -/
def cert_sig368 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig368_hard : cert_sig368.isHard = true := by native_decide

-- (3,6,9): κ=0.6111 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (3,6,9): κ = 0.611111. -/
def sig369 : BealFoundry.Signature := ⟨3, 6, 9, by omega, by omega, by omega⟩

theorem sig369_hyperbolic : sig369.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^9 up to 1000: no coprime solutions. -/
def cert_sig369 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig369_hard : cert_sig369.isHard = true := by native_decide

-- (3,6,10): κ=0.6000 [hyperbolic] solutions=[] bound=1000 [46.1ms]
/-- (3,6,10): κ = 0.600000. -/
def sig3610 : BealFoundry.Signature := ⟨3, 6, 10, by omega, by omega, by omega⟩

theorem sig3610_hyperbolic : sig3610.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^10 up to 1000: no coprime solutions. -/
def cert_sig3610 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3610_hard : cert_sig3610.isHard = true := by native_decide

-- (3,6,11): κ=0.5909 [hyperbolic] solutions=[] bound=1000 [46.2ms]
/-- (3,6,11): κ = 0.590909. -/
def sig3611 : BealFoundry.Signature := ⟨3, 6, 11, by omega, by omega, by omega⟩

theorem sig3611_hyperbolic : sig3611.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^11 up to 1000: no coprime solutions. -/
def cert_sig3611 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3611_hard : cert_sig3611.isHard = true := by native_decide

-- (3,6,12): κ=0.5833 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (3,6,12): κ = 0.583333. -/
def sig3612 : BealFoundry.Signature := ⟨3, 6, 12, by omega, by omega, by omega⟩

theorem sig3612_hyperbolic : sig3612.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^12 up to 1000: no coprime solutions. -/
def cert_sig3612 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3612_hard : cert_sig3612.isHard = true := by native_decide

-- (3,6,13): κ=0.5769 [hyperbolic] solutions=[] bound=1000 [46.4ms]
/-- (3,6,13): κ = 0.576923. -/
def sig3613 : BealFoundry.Signature := ⟨3, 6, 13, by omega, by omega, by omega⟩

theorem sig3613_hyperbolic : sig3613.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^13 up to 1000: no coprime solutions. -/
def cert_sig3613 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3613_hard : cert_sig3613.isHard = true := by native_decide

-- (3,6,14): κ=0.5714 [hyperbolic] solutions=[] bound=1000 [46.6ms]
/-- (3,6,14): κ = 0.571429. -/
def sig3614 : BealFoundry.Signature := ⟨3, 6, 14, by omega, by omega, by omega⟩

theorem sig3614_hyperbolic : sig3614.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^14 up to 1000: no coprime solutions. -/
def cert_sig3614 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3614_hard : cert_sig3614.isHard = true := by native_decide

-- (3,6,15): κ=0.5667 [hyperbolic] solutions=[] bound=1000 [46.0ms]
/-- (3,6,15): κ = 0.566667. -/
def sig3615 : BealFoundry.Signature := ⟨3, 6, 15, by omega, by omega, by omega⟩

theorem sig3615_hyperbolic : sig3615.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^6 = c^15 up to 1000: no coprime solutions. -/
def cert_sig3615 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3615_hard : cert_sig3615.isHard = true := by native_decide

-- (3,7,7): κ=0.6190 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (3,7,7): κ = 0.619048. -/
def sig377 : BealFoundry.Signature := ⟨3, 7, 7, by omega, by omega, by omega⟩

theorem sig377_hyperbolic : sig377.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^7 up to 1000: no coprime solutions. -/
def cert_sig377 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig377_hard : cert_sig377.isHard = true := by native_decide

-- (3,7,8): κ=0.6012 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (3,7,8): κ = 0.601190. -/
def sig378 : BealFoundry.Signature := ⟨3, 7, 8, by omega, by omega, by omega⟩

theorem sig378_hyperbolic : sig378.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^8 up to 1000: no coprime solutions. -/
def cert_sig378 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig378_hard : cert_sig378.isHard = true := by native_decide

-- (3,7,9): κ=0.5873 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (3,7,9): κ = 0.587302. -/
def sig379 : BealFoundry.Signature := ⟨3, 7, 9, by omega, by omega, by omega⟩

theorem sig379_hyperbolic : sig379.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^9 up to 1000: no coprime solutions. -/
def cert_sig379 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig379_hard : cert_sig379.isHard = true := by native_decide

-- (3,7,10): κ=0.5762 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (3,7,10): κ = 0.576190. -/
def sig3710 : BealFoundry.Signature := ⟨3, 7, 10, by omega, by omega, by omega⟩

theorem sig3710_hyperbolic : sig3710.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^10 up to 1000: no coprime solutions. -/
def cert_sig3710 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3710_hard : cert_sig3710.isHard = true := by native_decide

-- (3,7,11): κ=0.5671 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (3,7,11): κ = 0.567100. -/
def sig3711 : BealFoundry.Signature := ⟨3, 7, 11, by omega, by omega, by omega⟩

theorem sig3711_hyperbolic : sig3711.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^11 up to 1000: no coprime solutions. -/
def cert_sig3711 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3711_hard : cert_sig3711.isHard = true := by native_decide

-- (3,7,12): κ=0.5595 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (3,7,12): κ = 0.559524. -/
def sig3712 : BealFoundry.Signature := ⟨3, 7, 12, by omega, by omega, by omega⟩

theorem sig3712_hyperbolic : sig3712.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^12 up to 1000: no coprime solutions. -/
def cert_sig3712 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3712_hard : cert_sig3712.isHard = true := by native_decide

-- (3,7,13): κ=0.5531 [hyperbolic] solutions=[] bound=1000 [48.0ms]
/-- (3,7,13): κ = 0.553114. -/
def sig3713 : BealFoundry.Signature := ⟨3, 7, 13, by omega, by omega, by omega⟩

theorem sig3713_hyperbolic : sig3713.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^13 up to 1000: no coprime solutions. -/
def cert_sig3713 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3713_hard : cert_sig3713.isHard = true := by native_decide

-- (3,7,14): κ=0.5476 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (3,7,14): κ = 0.547619. -/
def sig3714 : BealFoundry.Signature := ⟨3, 7, 14, by omega, by omega, by omega⟩

theorem sig3714_hyperbolic : sig3714.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^14 up to 1000: no coprime solutions. -/
def cert_sig3714 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3714_hard : cert_sig3714.isHard = true := by native_decide

-- (3,7,15): κ=0.5429 [hyperbolic] solutions=[] bound=1000 [48.0ms]
/-- (3,7,15): κ = 0.542857. -/
def sig3715 : BealFoundry.Signature := ⟨3, 7, 15, by omega, by omega, by omega⟩

theorem sig3715_hyperbolic : sig3715.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^7 = c^15 up to 1000: no coprime solutions. -/
def cert_sig3715 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3715_hard : cert_sig3715.isHard = true := by native_decide

-- (3,8,8): κ=0.5833 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (3,8,8): κ = 0.583333. -/
def sig388 : BealFoundry.Signature := ⟨3, 8, 8, by omega, by omega, by omega⟩

theorem sig388_hyperbolic : sig388.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^8 up to 1000: no coprime solutions. -/
def cert_sig388 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig388_hard : cert_sig388.isHard = true := by native_decide

-- (3,8,9): κ=0.5694 [hyperbolic] solutions=[] bound=1000 [50.3ms]
/-- (3,8,9): κ = 0.569444. -/
def sig389 : BealFoundry.Signature := ⟨3, 8, 9, by omega, by omega, by omega⟩

theorem sig389_hyperbolic : sig389.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^9 up to 1000: no coprime solutions. -/
def cert_sig389 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig389_hard : cert_sig389.isHard = true := by native_decide

-- (3,8,10): κ=0.5583 [hyperbolic] solutions=[] bound=1000 [50.6ms]
/-- (3,8,10): κ = 0.558333. -/
def sig3810 : BealFoundry.Signature := ⟨3, 8, 10, by omega, by omega, by omega⟩

theorem sig3810_hyperbolic : sig3810.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^10 up to 1000: no coprime solutions. -/
def cert_sig3810 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3810_hard : cert_sig3810.isHard = true := by native_decide

-- (3,8,11): κ=0.5492 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (3,8,11): κ = 0.549242. -/
def sig3811 : BealFoundry.Signature := ⟨3, 8, 11, by omega, by omega, by omega⟩

theorem sig3811_hyperbolic : sig3811.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^11 up to 1000: no coprime solutions. -/
def cert_sig3811 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3811_hard : cert_sig3811.isHard = true := by native_decide

-- (3,8,12): κ=0.5417 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (3,8,12): κ = 0.541667. -/
def sig3812 : BealFoundry.Signature := ⟨3, 8, 12, by omega, by omega, by omega⟩

theorem sig3812_hyperbolic : sig3812.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^12 up to 1000: no coprime solutions. -/
def cert_sig3812 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3812_hard : cert_sig3812.isHard = true := by native_decide

-- (3,8,13): κ=0.5353 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (3,8,13): κ = 0.535256. -/
def sig3813 : BealFoundry.Signature := ⟨3, 8, 13, by omega, by omega, by omega⟩

theorem sig3813_hyperbolic : sig3813.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^13 up to 1000: no coprime solutions. -/
def cert_sig3813 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3813_hard : cert_sig3813.isHard = true := by native_decide

-- (3,8,14): κ=0.5298 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (3,8,14): κ = 0.529762. -/
def sig3814 : BealFoundry.Signature := ⟨3, 8, 14, by omega, by omega, by omega⟩

theorem sig3814_hyperbolic : sig3814.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^14 up to 1000: no coprime solutions. -/
def cert_sig3814 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3814_hard : cert_sig3814.isHard = true := by native_decide

-- (3,8,15): κ=0.5250 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (3,8,15): κ = 0.525000. -/
def sig3815 : BealFoundry.Signature := ⟨3, 8, 15, by omega, by omega, by omega⟩

theorem sig3815_hyperbolic : sig3815.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^8 = c^15 up to 1000: no coprime solutions. -/
def cert_sig3815 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3815_hard : cert_sig3815.isHard = true := by native_decide

-- (3,9,9): κ=0.5556 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (3,9,9): κ = 0.555556. -/
def sig399 : BealFoundry.Signature := ⟨3, 9, 9, by omega, by omega, by omega⟩

theorem sig399_hyperbolic : sig399.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^9 = c^9 up to 1000: no coprime solutions. -/
def cert_sig399 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig399_hard : cert_sig399.isHard = true := by native_decide

-- (3,9,10): κ=0.5444 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (3,9,10): κ = 0.544444. -/
def sig3910 : BealFoundry.Signature := ⟨3, 9, 10, by omega, by omega, by omega⟩

theorem sig3910_hyperbolic : sig3910.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^9 = c^10 up to 1000: no coprime solutions. -/
def cert_sig3910 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3910_hard : cert_sig3910.isHard = true := by native_decide

-- (3,9,11): κ=0.5354 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (3,9,11): κ = 0.535354. -/
def sig3911 : BealFoundry.Signature := ⟨3, 9, 11, by omega, by omega, by omega⟩

theorem sig3911_hyperbolic : sig3911.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^9 = c^11 up to 1000: no coprime solutions. -/
def cert_sig3911 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3911_hard : cert_sig3911.isHard = true := by native_decide

-- (3,9,12): κ=0.5278 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (3,9,12): κ = 0.527778. -/
def sig3912 : BealFoundry.Signature := ⟨3, 9, 12, by omega, by omega, by omega⟩

theorem sig3912_hyperbolic : sig3912.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^9 = c^12 up to 1000: no coprime solutions. -/
def cert_sig3912 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3912_hard : cert_sig3912.isHard = true := by native_decide

-- (3,9,13): κ=0.5214 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (3,9,13): κ = 0.521368. -/
def sig3913 : BealFoundry.Signature := ⟨3, 9, 13, by omega, by omega, by omega⟩

theorem sig3913_hyperbolic : sig3913.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^9 = c^13 up to 1000: no coprime solutions. -/
def cert_sig3913 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3913_hard : cert_sig3913.isHard = true := by native_decide

-- (3,9,14): κ=0.5159 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (3,9,14): κ = 0.515873. -/
def sig3914 : BealFoundry.Signature := ⟨3, 9, 14, by omega, by omega, by omega⟩

theorem sig3914_hyperbolic : sig3914.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^9 = c^14 up to 1000: no coprime solutions. -/
def cert_sig3914 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3914_hard : cert_sig3914.isHard = true := by native_decide

-- (3,9,15): κ=0.5111 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (3,9,15): κ = 0.511111. -/
def sig3915 : BealFoundry.Signature := ⟨3, 9, 15, by omega, by omega, by omega⟩

theorem sig3915_hyperbolic : sig3915.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^9 = c^15 up to 1000: no coprime solutions. -/
def cert_sig3915 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig3915_hard : cert_sig3915.isHard = true := by native_decide

-- (3,10,10): κ=0.5333 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (3,10,10): κ = 0.533333. -/
def sig31010 : BealFoundry.Signature := ⟨3, 10, 10, by omega, by omega, by omega⟩

theorem sig31010_hyperbolic : sig31010.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig31010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31010_hard : cert_sig31010.isHard = true := by native_decide

-- (3,10,11): κ=0.5242 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (3,10,11): κ = 0.524242. -/
def sig31011 : BealFoundry.Signature := ⟨3, 10, 11, by omega, by omega, by omega⟩

theorem sig31011_hyperbolic : sig31011.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig31011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31011_hard : cert_sig31011.isHard = true := by native_decide

-- (3,10,12): κ=0.5167 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (3,10,12): κ = 0.516667. -/
def sig31012 : BealFoundry.Signature := ⟨3, 10, 12, by omega, by omega, by omega⟩

theorem sig31012_hyperbolic : sig31012.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig31012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31012_hard : cert_sig31012.isHard = true := by native_decide

-- (3,10,13): κ=0.5103 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (3,10,13): κ = 0.510256. -/
def sig31013 : BealFoundry.Signature := ⟨3, 10, 13, by omega, by omega, by omega⟩

theorem sig31013_hyperbolic : sig31013.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig31013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31013_hard : cert_sig31013.isHard = true := by native_decide

-- (3,10,14): κ=0.5048 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (3,10,14): κ = 0.504762. -/
def sig31014 : BealFoundry.Signature := ⟨3, 10, 14, by omega, by omega, by omega⟩

theorem sig31014_hyperbolic : sig31014.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig31014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31014_hard : cert_sig31014.isHard = true := by native_decide

-- (3,10,15): κ=0.5000 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (3,10,15): κ = 0.500000. -/
def sig31015 : BealFoundry.Signature := ⟨3, 10, 15, by omega, by omega, by omega⟩

theorem sig31015_hyperbolic : sig31015.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig31015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31015_hard : cert_sig31015.isHard = true := by native_decide

-- (3,11,11): κ=0.5152 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (3,11,11): κ = 0.515152. -/
def sig31111 : BealFoundry.Signature := ⟨3, 11, 11, by omega, by omega, by omega⟩

theorem sig31111_hyperbolic : sig31111.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig31111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31111_hard : cert_sig31111.isHard = true := by native_decide

-- (3,11,12): κ=0.5076 [hyperbolic] solutions=[] bound=1000 [51.3ms]
/-- (3,11,12): κ = 0.507576. -/
def sig31112 : BealFoundry.Signature := ⟨3, 11, 12, by omega, by omega, by omega⟩

theorem sig31112_hyperbolic : sig31112.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig31112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31112_hard : cert_sig31112.isHard = true := by native_decide

-- (3,11,13): κ=0.5012 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (3,11,13): κ = 0.501166. -/
def sig31113 : BealFoundry.Signature := ⟨3, 11, 13, by omega, by omega, by omega⟩

theorem sig31113_hyperbolic : sig31113.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig31113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31113_hard : cert_sig31113.isHard = true := by native_decide

-- (3,11,14): κ=0.4957 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (3,11,14): κ = 0.495671. -/
def sig31114 : BealFoundry.Signature := ⟨3, 11, 14, by omega, by omega, by omega⟩

theorem sig31114_hyperbolic : sig31114.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig31114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31114_hard : cert_sig31114.isHard = true := by native_decide

-- (3,11,15): κ=0.4909 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (3,11,15): κ = 0.490909. -/
def sig31115 : BealFoundry.Signature := ⟨3, 11, 15, by omega, by omega, by omega⟩

theorem sig31115_hyperbolic : sig31115.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig31115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31115_hard : cert_sig31115.isHard = true := by native_decide

-- (3,12,12): κ=0.5000 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (3,12,12): κ = 0.500000. -/
def sig31212 : BealFoundry.Signature := ⟨3, 12, 12, by omega, by omega, by omega⟩

theorem sig31212_hyperbolic : sig31212.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig31212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31212_hard : cert_sig31212.isHard = true := by native_decide

-- (3,12,13): κ=0.4936 [hyperbolic] solutions=[] bound=1000 [52.8ms]
/-- (3,12,13): κ = 0.493590. -/
def sig31213 : BealFoundry.Signature := ⟨3, 12, 13, by omega, by omega, by omega⟩

theorem sig31213_hyperbolic : sig31213.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig31213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31213_hard : cert_sig31213.isHard = true := by native_decide

-- (3,12,14): κ=0.4881 [hyperbolic] solutions=[] bound=1000 [51.7ms]
/-- (3,12,14): κ = 0.488095. -/
def sig31214 : BealFoundry.Signature := ⟨3, 12, 14, by omega, by omega, by omega⟩

theorem sig31214_hyperbolic : sig31214.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig31214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31214_hard : cert_sig31214.isHard = true := by native_decide

-- (3,12,15): κ=0.4833 [hyperbolic] solutions=[] bound=1000 [51.1ms]
/-- (3,12,15): κ = 0.483333. -/
def sig31215 : BealFoundry.Signature := ⟨3, 12, 15, by omega, by omega, by omega⟩

theorem sig31215_hyperbolic : sig31215.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig31215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31215_hard : cert_sig31215.isHard = true := by native_decide

-- (3,13,13): κ=0.4872 [hyperbolic] solutions=[] bound=1000 [50.3ms]
/-- (3,13,13): κ = 0.487179. -/
def sig31313 : BealFoundry.Signature := ⟨3, 13, 13, by omega, by omega, by omega⟩

theorem sig31313_hyperbolic : sig31313.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig31313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31313_hard : cert_sig31313.isHard = true := by native_decide

-- (3,13,14): κ=0.4817 [hyperbolic] solutions=[] bound=1000 [50.3ms]
/-- (3,13,14): κ = 0.481685. -/
def sig31314 : BealFoundry.Signature := ⟨3, 13, 14, by omega, by omega, by omega⟩

theorem sig31314_hyperbolic : sig31314.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig31314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31314_hard : cert_sig31314.isHard = true := by native_decide

-- (3,13,15): κ=0.4769 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (3,13,15): κ = 0.476923. -/
def sig31315 : BealFoundry.Signature := ⟨3, 13, 15, by omega, by omega, by omega⟩

theorem sig31315_hyperbolic : sig31315.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig31315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31315_hard : cert_sig31315.isHard = true := by native_decide

-- (3,14,14): κ=0.4762 [hyperbolic] solutions=[] bound=1000 [52.3ms]
/-- (3,14,14): κ = 0.476190. -/
def sig31414 : BealFoundry.Signature := ⟨3, 14, 14, by omega, by omega, by omega⟩

theorem sig31414_hyperbolic : sig31414.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig31414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31414_hard : cert_sig31414.isHard = true := by native_decide

-- (3,14,15): κ=0.4714 [hyperbolic] solutions=[] bound=1000 [50.3ms]
/-- (3,14,15): κ = 0.471429. -/
def sig31415 : BealFoundry.Signature := ⟨3, 14, 15, by omega, by omega, by omega⟩

theorem sig31415_hyperbolic : sig31415.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig31415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31415_hard : cert_sig31415.isHard = true := by native_decide

-- (3,15,15): κ=0.4667 [hyperbolic] solutions=[] bound=1000 [50.5ms]
/-- (3,15,15): κ = 0.466667. -/
def sig31515 : BealFoundry.Signature := ⟨3, 15, 15, by omega, by omega, by omega⟩

theorem sig31515_hyperbolic : sig31515.regime = .hyperbolic := by
  native_decide

/-- Searched a^3 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig31515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig31515_hard : cert_sig31515.isHard = true := by native_decide

-- (4,4,4): κ=0.7500 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (4,4,4): κ = 0.750000. -/
def sig444 : BealFoundry.Signature := ⟨4, 4, 4, by omega, by omega, by omega⟩

theorem sig444_hyperbolic : sig444.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^4 up to 1000: no coprime solutions. -/
def cert_sig444 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig444_hard : cert_sig444.isHard = true := by native_decide

-- (4,4,5): κ=0.7000 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (4,4,5): κ = 0.700000. -/
def sig445 : BealFoundry.Signature := ⟨4, 4, 5, by omega, by omega, by omega⟩

theorem sig445_hyperbolic : sig445.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^5 up to 1000: no coprime solutions. -/
def cert_sig445 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig445_hard : cert_sig445.isHard = true := by native_decide

-- (4,4,6): κ=0.6667 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (4,4,6): κ = 0.666667. -/
def sig446 : BealFoundry.Signature := ⟨4, 4, 6, by omega, by omega, by omega⟩

theorem sig446_hyperbolic : sig446.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^6 up to 1000: no coprime solutions. -/
def cert_sig446 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig446_hard : cert_sig446.isHard = true := by native_decide

-- (4,4,7): κ=0.6429 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (4,4,7): κ = 0.642857. -/
def sig447 : BealFoundry.Signature := ⟨4, 4, 7, by omega, by omega, by omega⟩

theorem sig447_hyperbolic : sig447.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^7 up to 1000: no coprime solutions. -/
def cert_sig447 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig447_hard : cert_sig447.isHard = true := by native_decide

-- (4,4,8): κ=0.6250 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (4,4,8): κ = 0.625000. -/
def sig448 : BealFoundry.Signature := ⟨4, 4, 8, by omega, by omega, by omega⟩

theorem sig448_hyperbolic : sig448.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^8 up to 1000: no coprime solutions. -/
def cert_sig448 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig448_hard : cert_sig448.isHard = true := by native_decide

-- (4,4,9): κ=0.6111 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (4,4,9): κ = 0.611111. -/
def sig449 : BealFoundry.Signature := ⟨4, 4, 9, by omega, by omega, by omega⟩

theorem sig449_hyperbolic : sig449.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^9 up to 1000: no coprime solutions. -/
def cert_sig449 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig449_hard : cert_sig449.isHard = true := by native_decide

-- (4,4,10): κ=0.6000 [hyperbolic] solutions=[] bound=1000 [47.5ms]
/-- (4,4,10): κ = 0.600000. -/
def sig4410 : BealFoundry.Signature := ⟨4, 4, 10, by omega, by omega, by omega⟩

theorem sig4410_hyperbolic : sig4410.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^10 up to 1000: no coprime solutions. -/
def cert_sig4410 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4410_hard : cert_sig4410.isHard = true := by native_decide

-- (4,4,11): κ=0.5909 [hyperbolic] solutions=[] bound=1000 [47.8ms]
/-- (4,4,11): κ = 0.590909. -/
def sig4411 : BealFoundry.Signature := ⟨4, 4, 11, by omega, by omega, by omega⟩

theorem sig4411_hyperbolic : sig4411.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^11 up to 1000: no coprime solutions. -/
def cert_sig4411 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4411_hard : cert_sig4411.isHard = true := by native_decide

-- (4,4,12): κ=0.5833 [hyperbolic] solutions=[] bound=1000 [47.5ms]
/-- (4,4,12): κ = 0.583333. -/
def sig4412 : BealFoundry.Signature := ⟨4, 4, 12, by omega, by omega, by omega⟩

theorem sig4412_hyperbolic : sig4412.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^12 up to 1000: no coprime solutions. -/
def cert_sig4412 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4412_hard : cert_sig4412.isHard = true := by native_decide

-- (4,4,13): κ=0.5769 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (4,4,13): κ = 0.576923. -/
def sig4413 : BealFoundry.Signature := ⟨4, 4, 13, by omega, by omega, by omega⟩

theorem sig4413_hyperbolic : sig4413.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^13 up to 1000: no coprime solutions. -/
def cert_sig4413 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4413_hard : cert_sig4413.isHard = true := by native_decide

-- (4,4,14): κ=0.5714 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (4,4,14): κ = 0.571429. -/
def sig4414 : BealFoundry.Signature := ⟨4, 4, 14, by omega, by omega, by omega⟩

theorem sig4414_hyperbolic : sig4414.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^14 up to 1000: no coprime solutions. -/
def cert_sig4414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4414_hard : cert_sig4414.isHard = true := by native_decide

-- (4,4,15): κ=0.5667 [hyperbolic] solutions=[] bound=1000 [45.7ms]
/-- (4,4,15): κ = 0.566667. -/
def sig4415 : BealFoundry.Signature := ⟨4, 4, 15, by omega, by omega, by omega⟩

theorem sig4415_hyperbolic : sig4415.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^4 = c^15 up to 1000: no coprime solutions. -/
def cert_sig4415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4415_hard : cert_sig4415.isHard = true := by native_decide

-- (4,5,5): κ=0.6500 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (4,5,5): κ = 0.650000. -/
def sig455 : BealFoundry.Signature := ⟨4, 5, 5, by omega, by omega, by omega⟩

theorem sig455_hyperbolic : sig455.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^5 up to 1000: no coprime solutions. -/
def cert_sig455 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig455_hard : cert_sig455.isHard = true := by native_decide

-- (4,5,6): κ=0.6167 [hyperbolic] solutions=[] bound=1000 [46.5ms]
/-- (4,5,6): κ = 0.616667. -/
def sig456 : BealFoundry.Signature := ⟨4, 5, 6, by omega, by omega, by omega⟩

theorem sig456_hyperbolic : sig456.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^6 up to 1000: no coprime solutions. -/
def cert_sig456 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig456_hard : cert_sig456.isHard = true := by native_decide

-- (4,5,7): κ=0.5929 [hyperbolic] solutions=[] bound=1000 [47.3ms]
/-- (4,5,7): κ = 0.592857. -/
def sig457 : BealFoundry.Signature := ⟨4, 5, 7, by omega, by omega, by omega⟩

theorem sig457_hyperbolic : sig457.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^7 up to 1000: no coprime solutions. -/
def cert_sig457 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig457_hard : cert_sig457.isHard = true := by native_decide

-- (4,5,8): κ=0.5750 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (4,5,8): κ = 0.575000. -/
def sig458 : BealFoundry.Signature := ⟨4, 5, 8, by omega, by omega, by omega⟩

theorem sig458_hyperbolic : sig458.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^8 up to 1000: no coprime solutions. -/
def cert_sig458 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig458_hard : cert_sig458.isHard = true := by native_decide

-- (4,5,9): κ=0.5611 [hyperbolic] solutions=[] bound=1000 [47.8ms]
/-- (4,5,9): κ = 0.561111. -/
def sig459 : BealFoundry.Signature := ⟨4, 5, 9, by omega, by omega, by omega⟩

theorem sig459_hyperbolic : sig459.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^9 up to 1000: no coprime solutions. -/
def cert_sig459 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig459_hard : cert_sig459.isHard = true := by native_decide

-- (4,5,10): κ=0.5500 [hyperbolic] solutions=[] bound=1000 [47.1ms]
/-- (4,5,10): κ = 0.550000. -/
def sig4510 : BealFoundry.Signature := ⟨4, 5, 10, by omega, by omega, by omega⟩

theorem sig4510_hyperbolic : sig4510.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^10 up to 1000: no coprime solutions. -/
def cert_sig4510 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4510_hard : cert_sig4510.isHard = true := by native_decide

-- (4,5,11): κ=0.5409 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (4,5,11): κ = 0.540909. -/
def sig4511 : BealFoundry.Signature := ⟨4, 5, 11, by omega, by omega, by omega⟩

theorem sig4511_hyperbolic : sig4511.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^11 up to 1000: no coprime solutions. -/
def cert_sig4511 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4511_hard : cert_sig4511.isHard = true := by native_decide

-- (4,5,12): κ=0.5333 [hyperbolic] solutions=[] bound=1000 [47.9ms]
/-- (4,5,12): κ = 0.533333. -/
def sig4512 : BealFoundry.Signature := ⟨4, 5, 12, by omega, by omega, by omega⟩

theorem sig4512_hyperbolic : sig4512.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^12 up to 1000: no coprime solutions. -/
def cert_sig4512 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4512_hard : cert_sig4512.isHard = true := by native_decide

-- (4,5,13): κ=0.5269 [hyperbolic] solutions=[] bound=1000 [47.0ms]
/-- (4,5,13): κ = 0.526923. -/
def sig4513 : BealFoundry.Signature := ⟨4, 5, 13, by omega, by omega, by omega⟩

theorem sig4513_hyperbolic : sig4513.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^13 up to 1000: no coprime solutions. -/
def cert_sig4513 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4513_hard : cert_sig4513.isHard = true := by native_decide

-- (4,5,14): κ=0.5214 [hyperbolic] solutions=[] bound=1000 [46.8ms]
/-- (4,5,14): κ = 0.521429. -/
def sig4514 : BealFoundry.Signature := ⟨4, 5, 14, by omega, by omega, by omega⟩

theorem sig4514_hyperbolic : sig4514.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^14 up to 1000: no coprime solutions. -/
def cert_sig4514 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4514_hard : cert_sig4514.isHard = true := by native_decide

-- (4,5,15): κ=0.5167 [hyperbolic] solutions=[] bound=1000 [47.0ms]
/-- (4,5,15): κ = 0.516667. -/
def sig4515 : BealFoundry.Signature := ⟨4, 5, 15, by omega, by omega, by omega⟩

theorem sig4515_hyperbolic : sig4515.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^5 = c^15 up to 1000: no coprime solutions. -/
def cert_sig4515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4515_hard : cert_sig4515.isHard = true := by native_decide

-- (4,6,6): κ=0.5833 [hyperbolic] solutions=[] bound=1000 [46.8ms]
/-- (4,6,6): κ = 0.583333. -/
def sig466 : BealFoundry.Signature := ⟨4, 6, 6, by omega, by omega, by omega⟩

theorem sig466_hyperbolic : sig466.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^6 up to 1000: no coprime solutions. -/
def cert_sig466 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig466_hard : cert_sig466.isHard = true := by native_decide

-- (4,6,7): κ=0.5595 [hyperbolic] solutions=[] bound=1000 [46.3ms]
/-- (4,6,7): κ = 0.559524. -/
def sig467 : BealFoundry.Signature := ⟨4, 6, 7, by omega, by omega, by omega⟩

theorem sig467_hyperbolic : sig467.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^7 up to 1000: no coprime solutions. -/
def cert_sig467 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig467_hard : cert_sig467.isHard = true := by native_decide

-- (4,6,8): κ=0.5417 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (4,6,8): κ = 0.541667. -/
def sig468 : BealFoundry.Signature := ⟨4, 6, 8, by omega, by omega, by omega⟩

theorem sig468_hyperbolic : sig468.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^8 up to 1000: no coprime solutions. -/
def cert_sig468 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig468_hard : cert_sig468.isHard = true := by native_decide

-- (4,6,9): κ=0.5278 [hyperbolic] solutions=[] bound=1000 [46.8ms]
/-- (4,6,9): κ = 0.527778. -/
def sig469 : BealFoundry.Signature := ⟨4, 6, 9, by omega, by omega, by omega⟩

theorem sig469_hyperbolic : sig469.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^9 up to 1000: no coprime solutions. -/
def cert_sig469 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig469_hard : cert_sig469.isHard = true := by native_decide

-- (4,6,10): κ=0.5167 [hyperbolic] solutions=[] bound=1000 [45.8ms]
/-- (4,6,10): κ = 0.516667. -/
def sig4610 : BealFoundry.Signature := ⟨4, 6, 10, by omega, by omega, by omega⟩

theorem sig4610_hyperbolic : sig4610.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^10 up to 1000: no coprime solutions. -/
def cert_sig4610 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4610_hard : cert_sig4610.isHard = true := by native_decide

-- (4,6,11): κ=0.5076 [hyperbolic] solutions=[] bound=1000 [46.0ms]
/-- (4,6,11): κ = 0.507576. -/
def sig4611 : BealFoundry.Signature := ⟨4, 6, 11, by omega, by omega, by omega⟩

theorem sig4611_hyperbolic : sig4611.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^11 up to 1000: no coprime solutions. -/
def cert_sig4611 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4611_hard : cert_sig4611.isHard = true := by native_decide

-- (4,6,12): κ=0.5000 [hyperbolic] solutions=[] bound=1000 [46.5ms]
/-- (4,6,12): κ = 0.500000. -/
def sig4612 : BealFoundry.Signature := ⟨4, 6, 12, by omega, by omega, by omega⟩

theorem sig4612_hyperbolic : sig4612.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^12 up to 1000: no coprime solutions. -/
def cert_sig4612 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4612_hard : cert_sig4612.isHard = true := by native_decide

-- (4,6,13): κ=0.4936 [hyperbolic] solutions=[] bound=1000 [46.5ms]
/-- (4,6,13): κ = 0.493590. -/
def sig4613 : BealFoundry.Signature := ⟨4, 6, 13, by omega, by omega, by omega⟩

theorem sig4613_hyperbolic : sig4613.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^13 up to 1000: no coprime solutions. -/
def cert_sig4613 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4613_hard : cert_sig4613.isHard = true := by native_decide

-- (4,6,14): κ=0.4881 [hyperbolic] solutions=[] bound=1000 [46.4ms]
/-- (4,6,14): κ = 0.488095. -/
def sig4614 : BealFoundry.Signature := ⟨4, 6, 14, by omega, by omega, by omega⟩

theorem sig4614_hyperbolic : sig4614.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^14 up to 1000: no coprime solutions. -/
def cert_sig4614 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4614_hard : cert_sig4614.isHard = true := by native_decide

-- (4,6,15): κ=0.4833 [hyperbolic] solutions=[] bound=1000 [46.2ms]
/-- (4,6,15): κ = 0.483333. -/
def sig4615 : BealFoundry.Signature := ⟨4, 6, 15, by omega, by omega, by omega⟩

theorem sig4615_hyperbolic : sig4615.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^6 = c^15 up to 1000: no coprime solutions. -/
def cert_sig4615 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4615_hard : cert_sig4615.isHard = true := by native_decide

-- (4,7,7): κ=0.5357 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (4,7,7): κ = 0.535714. -/
def sig477 : BealFoundry.Signature := ⟨4, 7, 7, by omega, by omega, by omega⟩

theorem sig477_hyperbolic : sig477.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^7 up to 1000: no coprime solutions. -/
def cert_sig477 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig477_hard : cert_sig477.isHard = true := by native_decide

-- (4,7,8): κ=0.5179 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (4,7,8): κ = 0.517857. -/
def sig478 : BealFoundry.Signature := ⟨4, 7, 8, by omega, by omega, by omega⟩

theorem sig478_hyperbolic : sig478.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^8 up to 1000: no coprime solutions. -/
def cert_sig478 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig478_hard : cert_sig478.isHard = true := by native_decide

-- (4,7,9): κ=0.5040 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (4,7,9): κ = 0.503968. -/
def sig479 : BealFoundry.Signature := ⟨4, 7, 9, by omega, by omega, by omega⟩

theorem sig479_hyperbolic : sig479.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^9 up to 1000: no coprime solutions. -/
def cert_sig479 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig479_hard : cert_sig479.isHard = true := by native_decide

-- (4,7,10): κ=0.4929 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (4,7,10): κ = 0.492857. -/
def sig4710 : BealFoundry.Signature := ⟨4, 7, 10, by omega, by omega, by omega⟩

theorem sig4710_hyperbolic : sig4710.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^10 up to 1000: no coprime solutions. -/
def cert_sig4710 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4710_hard : cert_sig4710.isHard = true := by native_decide

-- (4,7,11): κ=0.4838 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (4,7,11): κ = 0.483766. -/
def sig4711 : BealFoundry.Signature := ⟨4, 7, 11, by omega, by omega, by omega⟩

theorem sig4711_hyperbolic : sig4711.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^11 up to 1000: no coprime solutions. -/
def cert_sig4711 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4711_hard : cert_sig4711.isHard = true := by native_decide

-- (4,7,12): κ=0.4762 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (4,7,12): κ = 0.476190. -/
def sig4712 : BealFoundry.Signature := ⟨4, 7, 12, by omega, by omega, by omega⟩

theorem sig4712_hyperbolic : sig4712.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^12 up to 1000: no coprime solutions. -/
def cert_sig4712 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4712_hard : cert_sig4712.isHard = true := by native_decide

-- (4,7,13): κ=0.4698 [hyperbolic] solutions=[] bound=1000 [47.8ms]
/-- (4,7,13): κ = 0.469780. -/
def sig4713 : BealFoundry.Signature := ⟨4, 7, 13, by omega, by omega, by omega⟩

theorem sig4713_hyperbolic : sig4713.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^13 up to 1000: no coprime solutions. -/
def cert_sig4713 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4713_hard : cert_sig4713.isHard = true := by native_decide

-- (4,7,14): κ=0.4643 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (4,7,14): κ = 0.464286. -/
def sig4714 : BealFoundry.Signature := ⟨4, 7, 14, by omega, by omega, by omega⟩

theorem sig4714_hyperbolic : sig4714.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^14 up to 1000: no coprime solutions. -/
def cert_sig4714 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4714_hard : cert_sig4714.isHard = true := by native_decide

-- (4,7,15): κ=0.4595 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (4,7,15): κ = 0.459524. -/
def sig4715 : BealFoundry.Signature := ⟨4, 7, 15, by omega, by omega, by omega⟩

theorem sig4715_hyperbolic : sig4715.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^7 = c^15 up to 1000: no coprime solutions. -/
def cert_sig4715 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4715_hard : cert_sig4715.isHard = true := by native_decide

-- (4,8,8): κ=0.5000 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (4,8,8): κ = 0.500000. -/
def sig488 : BealFoundry.Signature := ⟨4, 8, 8, by omega, by omega, by omega⟩

theorem sig488_hyperbolic : sig488.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^8 up to 1000: no coprime solutions. -/
def cert_sig488 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig488_hard : cert_sig488.isHard = true := by native_decide

-- (4,8,9): κ=0.4861 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (4,8,9): κ = 0.486111. -/
def sig489 : BealFoundry.Signature := ⟨4, 8, 9, by omega, by omega, by omega⟩

theorem sig489_hyperbolic : sig489.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^9 up to 1000: no coprime solutions. -/
def cert_sig489 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig489_hard : cert_sig489.isHard = true := by native_decide

-- (4,8,10): κ=0.4750 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (4,8,10): κ = 0.475000. -/
def sig4810 : BealFoundry.Signature := ⟨4, 8, 10, by omega, by omega, by omega⟩

theorem sig4810_hyperbolic : sig4810.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^10 up to 1000: no coprime solutions. -/
def cert_sig4810 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4810_hard : cert_sig4810.isHard = true := by native_decide

-- (4,8,11): κ=0.4659 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (4,8,11): κ = 0.465909. -/
def sig4811 : BealFoundry.Signature := ⟨4, 8, 11, by omega, by omega, by omega⟩

theorem sig4811_hyperbolic : sig4811.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^11 up to 1000: no coprime solutions. -/
def cert_sig4811 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4811_hard : cert_sig4811.isHard = true := by native_decide

-- (4,8,12): κ=0.4583 [hyperbolic] solutions=[] bound=1000 [54.0ms]
/-- (4,8,12): κ = 0.458333. -/
def sig4812 : BealFoundry.Signature := ⟨4, 8, 12, by omega, by omega, by omega⟩

theorem sig4812_hyperbolic : sig4812.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^12 up to 1000: no coprime solutions. -/
def cert_sig4812 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4812_hard : cert_sig4812.isHard = true := by native_decide

-- (4,8,13): κ=0.4519 [hyperbolic] solutions=[] bound=1000 [55.5ms]
/-- (4,8,13): κ = 0.451923. -/
def sig4813 : BealFoundry.Signature := ⟨4, 8, 13, by omega, by omega, by omega⟩

theorem sig4813_hyperbolic : sig4813.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^13 up to 1000: no coprime solutions. -/
def cert_sig4813 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4813_hard : cert_sig4813.isHard = true := by native_decide

-- (4,8,14): κ=0.4464 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (4,8,14): κ = 0.446429. -/
def sig4814 : BealFoundry.Signature := ⟨4, 8, 14, by omega, by omega, by omega⟩

theorem sig4814_hyperbolic : sig4814.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^14 up to 1000: no coprime solutions. -/
def cert_sig4814 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4814_hard : cert_sig4814.isHard = true := by native_decide

-- (4,8,15): κ=0.4417 [hyperbolic] solutions=[] bound=1000 [50.6ms]
/-- (4,8,15): κ = 0.441667. -/
def sig4815 : BealFoundry.Signature := ⟨4, 8, 15, by omega, by omega, by omega⟩

theorem sig4815_hyperbolic : sig4815.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^8 = c^15 up to 1000: no coprime solutions. -/
def cert_sig4815 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4815_hard : cert_sig4815.isHard = true := by native_decide

-- (4,9,9): κ=0.4722 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (4,9,9): κ = 0.472222. -/
def sig499 : BealFoundry.Signature := ⟨4, 9, 9, by omega, by omega, by omega⟩

theorem sig499_hyperbolic : sig499.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^9 = c^9 up to 1000: no coprime solutions. -/
def cert_sig499 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig499_hard : cert_sig499.isHard = true := by native_decide

-- (4,9,10): κ=0.4611 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (4,9,10): κ = 0.461111. -/
def sig4910 : BealFoundry.Signature := ⟨4, 9, 10, by omega, by omega, by omega⟩

theorem sig4910_hyperbolic : sig4910.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^9 = c^10 up to 1000: no coprime solutions. -/
def cert_sig4910 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4910_hard : cert_sig4910.isHard = true := by native_decide

-- (4,9,11): κ=0.4520 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (4,9,11): κ = 0.452020. -/
def sig4911 : BealFoundry.Signature := ⟨4, 9, 11, by omega, by omega, by omega⟩

theorem sig4911_hyperbolic : sig4911.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^9 = c^11 up to 1000: no coprime solutions. -/
def cert_sig4911 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4911_hard : cert_sig4911.isHard = true := by native_decide

-- (4,9,12): κ=0.4444 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (4,9,12): κ = 0.444444. -/
def sig4912 : BealFoundry.Signature := ⟨4, 9, 12, by omega, by omega, by omega⟩

theorem sig4912_hyperbolic : sig4912.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^9 = c^12 up to 1000: no coprime solutions. -/
def cert_sig4912 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4912_hard : cert_sig4912.isHard = true := by native_decide

-- (4,9,13): κ=0.4380 [hyperbolic] solutions=[] bound=1000 [47.9ms]
/-- (4,9,13): κ = 0.438034. -/
def sig4913 : BealFoundry.Signature := ⟨4, 9, 13, by omega, by omega, by omega⟩

theorem sig4913_hyperbolic : sig4913.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^9 = c^13 up to 1000: no coprime solutions. -/
def cert_sig4913 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4913_hard : cert_sig4913.isHard = true := by native_decide

-- (4,9,14): κ=0.4325 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (4,9,14): κ = 0.432540. -/
def sig4914 : BealFoundry.Signature := ⟨4, 9, 14, by omega, by omega, by omega⟩

theorem sig4914_hyperbolic : sig4914.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^9 = c^14 up to 1000: no coprime solutions. -/
def cert_sig4914 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4914_hard : cert_sig4914.isHard = true := by native_decide

-- (4,9,15): κ=0.4278 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (4,9,15): κ = 0.427778. -/
def sig4915 : BealFoundry.Signature := ⟨4, 9, 15, by omega, by omega, by omega⟩

theorem sig4915_hyperbolic : sig4915.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^9 = c^15 up to 1000: no coprime solutions. -/
def cert_sig4915 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig4915_hard : cert_sig4915.isHard = true := by native_decide

-- (4,10,10): κ=0.4500 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (4,10,10): κ = 0.450000. -/
def sig41010 : BealFoundry.Signature := ⟨4, 10, 10, by omega, by omega, by omega⟩

theorem sig41010_hyperbolic : sig41010.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig41010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41010_hard : cert_sig41010.isHard = true := by native_decide

-- (4,10,11): κ=0.4409 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (4,10,11): κ = 0.440909. -/
def sig41011 : BealFoundry.Signature := ⟨4, 10, 11, by omega, by omega, by omega⟩

theorem sig41011_hyperbolic : sig41011.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig41011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41011_hard : cert_sig41011.isHard = true := by native_decide

-- (4,10,12): κ=0.4333 [hyperbolic] solutions=[] bound=1000 [50.5ms]
/-- (4,10,12): κ = 0.433333. -/
def sig41012 : BealFoundry.Signature := ⟨4, 10, 12, by omega, by omega, by omega⟩

theorem sig41012_hyperbolic : sig41012.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig41012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41012_hard : cert_sig41012.isHard = true := by native_decide

-- (4,10,13): κ=0.4269 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (4,10,13): κ = 0.426923. -/
def sig41013 : BealFoundry.Signature := ⟨4, 10, 13, by omega, by omega, by omega⟩

theorem sig41013_hyperbolic : sig41013.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig41013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41013_hard : cert_sig41013.isHard = true := by native_decide

-- (4,10,14): κ=0.4214 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (4,10,14): κ = 0.421429. -/
def sig41014 : BealFoundry.Signature := ⟨4, 10, 14, by omega, by omega, by omega⟩

theorem sig41014_hyperbolic : sig41014.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig41014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41014_hard : cert_sig41014.isHard = true := by native_decide

-- (4,10,15): κ=0.4167 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (4,10,15): κ = 0.416667. -/
def sig41015 : BealFoundry.Signature := ⟨4, 10, 15, by omega, by omega, by omega⟩

theorem sig41015_hyperbolic : sig41015.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig41015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41015_hard : cert_sig41015.isHard = true := by native_decide

-- (4,11,11): κ=0.4318 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (4,11,11): κ = 0.431818. -/
def sig41111 : BealFoundry.Signature := ⟨4, 11, 11, by omega, by omega, by omega⟩

theorem sig41111_hyperbolic : sig41111.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig41111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41111_hard : cert_sig41111.isHard = true := by native_decide

-- (4,11,12): κ=0.4242 [hyperbolic] solutions=[] bound=1000 [51.0ms]
/-- (4,11,12): κ = 0.424242. -/
def sig41112 : BealFoundry.Signature := ⟨4, 11, 12, by omega, by omega, by omega⟩

theorem sig41112_hyperbolic : sig41112.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig41112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41112_hard : cert_sig41112.isHard = true := by native_decide

-- (4,11,13): κ=0.4178 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (4,11,13): κ = 0.417832. -/
def sig41113 : BealFoundry.Signature := ⟨4, 11, 13, by omega, by omega, by omega⟩

theorem sig41113_hyperbolic : sig41113.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig41113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41113_hard : cert_sig41113.isHard = true := by native_decide

-- (4,11,14): κ=0.4123 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (4,11,14): κ = 0.412338. -/
def sig41114 : BealFoundry.Signature := ⟨4, 11, 14, by omega, by omega, by omega⟩

theorem sig41114_hyperbolic : sig41114.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig41114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41114_hard : cert_sig41114.isHard = true := by native_decide

-- (4,11,15): κ=0.4076 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (4,11,15): κ = 0.407576. -/
def sig41115 : BealFoundry.Signature := ⟨4, 11, 15, by omega, by omega, by omega⟩

theorem sig41115_hyperbolic : sig41115.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig41115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41115_hard : cert_sig41115.isHard = true := by native_decide

-- (4,12,12): κ=0.4167 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (4,12,12): κ = 0.416667. -/
def sig41212 : BealFoundry.Signature := ⟨4, 12, 12, by omega, by omega, by omega⟩

theorem sig41212_hyperbolic : sig41212.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig41212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41212_hard : cert_sig41212.isHard = true := by native_decide

-- (4,12,13): κ=0.4103 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (4,12,13): κ = 0.410256. -/
def sig41213 : BealFoundry.Signature := ⟨4, 12, 13, by omega, by omega, by omega⟩

theorem sig41213_hyperbolic : sig41213.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig41213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41213_hard : cert_sig41213.isHard = true := by native_decide

-- (4,12,14): κ=0.4048 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (4,12,14): κ = 0.404762. -/
def sig41214 : BealFoundry.Signature := ⟨4, 12, 14, by omega, by omega, by omega⟩

theorem sig41214_hyperbolic : sig41214.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig41214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41214_hard : cert_sig41214.isHard = true := by native_decide

-- (4,12,15): κ=0.4000 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (4,12,15): κ = 0.400000. -/
def sig41215 : BealFoundry.Signature := ⟨4, 12, 15, by omega, by omega, by omega⟩

theorem sig41215_hyperbolic : sig41215.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig41215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41215_hard : cert_sig41215.isHard = true := by native_decide

-- (4,13,13): κ=0.4038 [hyperbolic] solutions=[] bound=1000 [50.6ms]
/-- (4,13,13): κ = 0.403846. -/
def sig41313 : BealFoundry.Signature := ⟨4, 13, 13, by omega, by omega, by omega⟩

theorem sig41313_hyperbolic : sig41313.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig41313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41313_hard : cert_sig41313.isHard = true := by native_decide

-- (4,13,14): κ=0.3984 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (4,13,14): κ = 0.398352. -/
def sig41314 : BealFoundry.Signature := ⟨4, 13, 14, by omega, by omega, by omega⟩

theorem sig41314_hyperbolic : sig41314.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig41314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41314_hard : cert_sig41314.isHard = true := by native_decide

-- (4,13,15): κ=0.3936 [hyperbolic] solutions=[] bound=1000 [50.6ms]
/-- (4,13,15): κ = 0.393590. -/
def sig41315 : BealFoundry.Signature := ⟨4, 13, 15, by omega, by omega, by omega⟩

theorem sig41315_hyperbolic : sig41315.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig41315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41315_hard : cert_sig41315.isHard = true := by native_decide

-- (4,14,14): κ=0.3929 [hyperbolic] solutions=[] bound=1000 [52.2ms]
/-- (4,14,14): κ = 0.392857. -/
def sig41414 : BealFoundry.Signature := ⟨4, 14, 14, by omega, by omega, by omega⟩

theorem sig41414_hyperbolic : sig41414.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig41414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41414_hard : cert_sig41414.isHard = true := by native_decide

-- (4,14,15): κ=0.3881 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (4,14,15): κ = 0.388095. -/
def sig41415 : BealFoundry.Signature := ⟨4, 14, 15, by omega, by omega, by omega⟩

theorem sig41415_hyperbolic : sig41415.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig41415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41415_hard : cert_sig41415.isHard = true := by native_decide

-- (4,15,15): κ=0.3833 [hyperbolic] solutions=[] bound=1000 [50.0ms]
/-- (4,15,15): κ = 0.383333. -/
def sig41515 : BealFoundry.Signature := ⟨4, 15, 15, by omega, by omega, by omega⟩

theorem sig41515_hyperbolic : sig41515.regime = .hyperbolic := by
  native_decide

/-- Searched a^4 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig41515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig41515_hard : cert_sig41515.isHard = true := by native_decide

-- (5,5,5): κ=0.6000 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (5,5,5): κ = 0.600000. -/
def sig555 : BealFoundry.Signature := ⟨5, 5, 5, by omega, by omega, by omega⟩

theorem sig555_hyperbolic : sig555.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^5 up to 1000: no coprime solutions. -/
def cert_sig555 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig555_hard : cert_sig555.isHard = true := by native_decide

-- (5,5,6): κ=0.5667 [hyperbolic] solutions=[] bound=1000 [46.0ms]
/-- (5,5,6): κ = 0.566667. -/
def sig556 : BealFoundry.Signature := ⟨5, 5, 6, by omega, by omega, by omega⟩

theorem sig556_hyperbolic : sig556.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^6 up to 1000: no coprime solutions. -/
def cert_sig556 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig556_hard : cert_sig556.isHard = true := by native_decide

-- (5,5,7): κ=0.5429 [hyperbolic] solutions=[] bound=1000 [47.6ms]
/-- (5,5,7): κ = 0.542857. -/
def sig557 : BealFoundry.Signature := ⟨5, 5, 7, by omega, by omega, by omega⟩

theorem sig557_hyperbolic : sig557.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^7 up to 1000: no coprime solutions. -/
def cert_sig557 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig557_hard : cert_sig557.isHard = true := by native_decide

-- (5,5,8): κ=0.5250 [hyperbolic] solutions=[] bound=1000 [47.7ms]
/-- (5,5,8): κ = 0.525000. -/
def sig558 : BealFoundry.Signature := ⟨5, 5, 8, by omega, by omega, by omega⟩

theorem sig558_hyperbolic : sig558.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^8 up to 1000: no coprime solutions. -/
def cert_sig558 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig558_hard : cert_sig558.isHard = true := by native_decide

-- (5,5,9): κ=0.5111 [hyperbolic] solutions=[] bound=1000 [47.4ms]
/-- (5,5,9): κ = 0.511111. -/
def sig559 : BealFoundry.Signature := ⟨5, 5, 9, by omega, by omega, by omega⟩

theorem sig559_hyperbolic : sig559.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^9 up to 1000: no coprime solutions. -/
def cert_sig559 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig559_hard : cert_sig559.isHard = true := by native_decide

-- (5,5,10): κ=0.5000 [hyperbolic] solutions=[] bound=1000 [47.4ms]
/-- (5,5,10): κ = 0.500000. -/
def sig5510 : BealFoundry.Signature := ⟨5, 5, 10, by omega, by omega, by omega⟩

theorem sig5510_hyperbolic : sig5510.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^10 up to 1000: no coprime solutions. -/
def cert_sig5510 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5510_hard : cert_sig5510.isHard = true := by native_decide

-- (5,5,11): κ=0.4909 [hyperbolic] solutions=[] bound=1000 [47.6ms]
/-- (5,5,11): κ = 0.490909. -/
def sig5511 : BealFoundry.Signature := ⟨5, 5, 11, by omega, by omega, by omega⟩

theorem sig5511_hyperbolic : sig5511.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^11 up to 1000: no coprime solutions. -/
def cert_sig5511 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5511_hard : cert_sig5511.isHard = true := by native_decide

-- (5,5,12): κ=0.4833 [hyperbolic] solutions=[] bound=1000 [47.5ms]
/-- (5,5,12): κ = 0.483333. -/
def sig5512 : BealFoundry.Signature := ⟨5, 5, 12, by omega, by omega, by omega⟩

theorem sig5512_hyperbolic : sig5512.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^12 up to 1000: no coprime solutions. -/
def cert_sig5512 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5512_hard : cert_sig5512.isHard = true := by native_decide

-- (5,5,13): κ=0.4769 [hyperbolic] solutions=[] bound=1000 [47.0ms]
/-- (5,5,13): κ = 0.476923. -/
def sig5513 : BealFoundry.Signature := ⟨5, 5, 13, by omega, by omega, by omega⟩

theorem sig5513_hyperbolic : sig5513.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^13 up to 1000: no coprime solutions. -/
def cert_sig5513 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5513_hard : cert_sig5513.isHard = true := by native_decide

-- (5,5,14): κ=0.4714 [hyperbolic] solutions=[] bound=1000 [47.3ms]
/-- (5,5,14): κ = 0.471429. -/
def sig5514 : BealFoundry.Signature := ⟨5, 5, 14, by omega, by omega, by omega⟩

theorem sig5514_hyperbolic : sig5514.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^14 up to 1000: no coprime solutions. -/
def cert_sig5514 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5514_hard : cert_sig5514.isHard = true := by native_decide

-- (5,5,15): κ=0.4667 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (5,5,15): κ = 0.466667. -/
def sig5515 : BealFoundry.Signature := ⟨5, 5, 15, by omega, by omega, by omega⟩

theorem sig5515_hyperbolic : sig5515.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^5 = c^15 up to 1000: no coprime solutions. -/
def cert_sig5515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5515_hard : cert_sig5515.isHard = true := by native_decide

-- (5,6,6): κ=0.5333 [hyperbolic] solutions=[] bound=1000 [45.6ms]
/-- (5,6,6): κ = 0.533333. -/
def sig566 : BealFoundry.Signature := ⟨5, 6, 6, by omega, by omega, by omega⟩

theorem sig566_hyperbolic : sig566.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^6 up to 1000: no coprime solutions. -/
def cert_sig566 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig566_hard : cert_sig566.isHard = true := by native_decide

-- (5,6,7): κ=0.5095 [hyperbolic] solutions=[] bound=1000 [46.5ms]
/-- (5,6,7): κ = 0.509524. -/
def sig567 : BealFoundry.Signature := ⟨5, 6, 7, by omega, by omega, by omega⟩

theorem sig567_hyperbolic : sig567.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^7 up to 1000: no coprime solutions. -/
def cert_sig567 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig567_hard : cert_sig567.isHard = true := by native_decide

-- (5,6,8): κ=0.4917 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (5,6,8): κ = 0.491667. -/
def sig568 : BealFoundry.Signature := ⟨5, 6, 8, by omega, by omega, by omega⟩

theorem sig568_hyperbolic : sig568.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^8 up to 1000: no coprime solutions. -/
def cert_sig568 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig568_hard : cert_sig568.isHard = true := by native_decide

-- (5,6,9): κ=0.4778 [hyperbolic] solutions=[] bound=1000 [47.0ms]
/-- (5,6,9): κ = 0.477778. -/
def sig569 : BealFoundry.Signature := ⟨5, 6, 9, by omega, by omega, by omega⟩

theorem sig569_hyperbolic : sig569.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^9 up to 1000: no coprime solutions. -/
def cert_sig569 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig569_hard : cert_sig569.isHard = true := by native_decide

-- (5,6,10): κ=0.4667 [hyperbolic] solutions=[] bound=1000 [46.2ms]
/-- (5,6,10): κ = 0.466667. -/
def sig5610 : BealFoundry.Signature := ⟨5, 6, 10, by omega, by omega, by omega⟩

theorem sig5610_hyperbolic : sig5610.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^10 up to 1000: no coprime solutions. -/
def cert_sig5610 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5610_hard : cert_sig5610.isHard = true := by native_decide

-- (5,6,11): κ=0.4576 [hyperbolic] solutions=[] bound=1000 [46.3ms]
/-- (5,6,11): κ = 0.457576. -/
def sig5611 : BealFoundry.Signature := ⟨5, 6, 11, by omega, by omega, by omega⟩

theorem sig5611_hyperbolic : sig5611.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^11 up to 1000: no coprime solutions. -/
def cert_sig5611 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5611_hard : cert_sig5611.isHard = true := by native_decide

-- (5,6,12): κ=0.4500 [hyperbolic] solutions=[] bound=1000 [46.4ms]
/-- (5,6,12): κ = 0.450000. -/
def sig5612 : BealFoundry.Signature := ⟨5, 6, 12, by omega, by omega, by omega⟩

theorem sig5612_hyperbolic : sig5612.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^12 up to 1000: no coprime solutions. -/
def cert_sig5612 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5612_hard : cert_sig5612.isHard = true := by native_decide

-- (5,6,13): κ=0.4436 [hyperbolic] solutions=[] bound=1000 [45.8ms]
/-- (5,6,13): κ = 0.443590. -/
def sig5613 : BealFoundry.Signature := ⟨5, 6, 13, by omega, by omega, by omega⟩

theorem sig5613_hyperbolic : sig5613.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^13 up to 1000: no coprime solutions. -/
def cert_sig5613 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5613_hard : cert_sig5613.isHard = true := by native_decide

-- (5,6,14): κ=0.4381 [hyperbolic] solutions=[] bound=1000 [46.2ms]
/-- (5,6,14): κ = 0.438095. -/
def sig5614 : BealFoundry.Signature := ⟨5, 6, 14, by omega, by omega, by omega⟩

theorem sig5614_hyperbolic : sig5614.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^14 up to 1000: no coprime solutions. -/
def cert_sig5614 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5614_hard : cert_sig5614.isHard = true := by native_decide

-- (5,6,15): κ=0.4333 [hyperbolic] solutions=[] bound=1000 [45.8ms]
/-- (5,6,15): κ = 0.433333. -/
def sig5615 : BealFoundry.Signature := ⟨5, 6, 15, by omega, by omega, by omega⟩

theorem sig5615_hyperbolic : sig5615.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^6 = c^15 up to 1000: no coprime solutions. -/
def cert_sig5615 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5615_hard : cert_sig5615.isHard = true := by native_decide

-- (5,7,7): κ=0.4857 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (5,7,7): κ = 0.485714. -/
def sig577 : BealFoundry.Signature := ⟨5, 7, 7, by omega, by omega, by omega⟩

theorem sig577_hyperbolic : sig577.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^7 up to 1000: no coprime solutions. -/
def cert_sig577 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig577_hard : cert_sig577.isHard = true := by native_decide

-- (5,7,8): κ=0.4679 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (5,7,8): κ = 0.467857. -/
def sig578 : BealFoundry.Signature := ⟨5, 7, 8, by omega, by omega, by omega⟩

theorem sig578_hyperbolic : sig578.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^8 up to 1000: no coprime solutions. -/
def cert_sig578 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig578_hard : cert_sig578.isHard = true := by native_decide

-- (5,7,9): κ=0.4540 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (5,7,9): κ = 0.453968. -/
def sig579 : BealFoundry.Signature := ⟨5, 7, 9, by omega, by omega, by omega⟩

theorem sig579_hyperbolic : sig579.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^9 up to 1000: no coprime solutions. -/
def cert_sig579 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig579_hard : cert_sig579.isHard = true := by native_decide

-- (5,7,10): κ=0.4429 [hyperbolic] solutions=[] bound=1000 [47.3ms]
/-- (5,7,10): κ = 0.442857. -/
def sig5710 : BealFoundry.Signature := ⟨5, 7, 10, by omega, by omega, by omega⟩

theorem sig5710_hyperbolic : sig5710.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^10 up to 1000: no coprime solutions. -/
def cert_sig5710 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5710_hard : cert_sig5710.isHard = true := by native_decide

-- (5,7,11): κ=0.4338 [hyperbolic] solutions=[] bound=1000 [47.9ms]
/-- (5,7,11): κ = 0.433766. -/
def sig5711 : BealFoundry.Signature := ⟨5, 7, 11, by omega, by omega, by omega⟩

theorem sig5711_hyperbolic : sig5711.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^11 up to 1000: no coprime solutions. -/
def cert_sig5711 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5711_hard : cert_sig5711.isHard = true := by native_decide

-- (5,7,12): κ=0.4262 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (5,7,12): κ = 0.426190. -/
def sig5712 : BealFoundry.Signature := ⟨5, 7, 12, by omega, by omega, by omega⟩

theorem sig5712_hyperbolic : sig5712.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^12 up to 1000: no coprime solutions. -/
def cert_sig5712 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5712_hard : cert_sig5712.isHard = true := by native_decide

-- (5,7,13): κ=0.4198 [hyperbolic] solutions=[] bound=1000 [47.5ms]
/-- (5,7,13): κ = 0.419780. -/
def sig5713 : BealFoundry.Signature := ⟨5, 7, 13, by omega, by omega, by omega⟩

theorem sig5713_hyperbolic : sig5713.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^13 up to 1000: no coprime solutions. -/
def cert_sig5713 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5713_hard : cert_sig5713.isHard = true := by native_decide

-- (5,7,14): κ=0.4143 [hyperbolic] solutions=[] bound=1000 [47.7ms]
/-- (5,7,14): κ = 0.414286. -/
def sig5714 : BealFoundry.Signature := ⟨5, 7, 14, by omega, by omega, by omega⟩

theorem sig5714_hyperbolic : sig5714.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^14 up to 1000: no coprime solutions. -/
def cert_sig5714 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5714_hard : cert_sig5714.isHard = true := by native_decide

-- (5,7,15): κ=0.4095 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (5,7,15): κ = 0.409524. -/
def sig5715 : BealFoundry.Signature := ⟨5, 7, 15, by omega, by omega, by omega⟩

theorem sig5715_hyperbolic : sig5715.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^7 = c^15 up to 1000: no coprime solutions. -/
def cert_sig5715 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5715_hard : cert_sig5715.isHard = true := by native_decide

-- (5,8,8): κ=0.4500 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (5,8,8): κ = 0.450000. -/
def sig588 : BealFoundry.Signature := ⟨5, 8, 8, by omega, by omega, by omega⟩

theorem sig588_hyperbolic : sig588.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^8 up to 1000: no coprime solutions. -/
def cert_sig588 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig588_hard : cert_sig588.isHard = true := by native_decide

-- (5,8,9): κ=0.4361 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (5,8,9): κ = 0.436111. -/
def sig589 : BealFoundry.Signature := ⟨5, 8, 9, by omega, by omega, by omega⟩

theorem sig589_hyperbolic : sig589.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^9 up to 1000: no coprime solutions. -/
def cert_sig589 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig589_hard : cert_sig589.isHard = true := by native_decide

-- (5,8,10): κ=0.4250 [hyperbolic] solutions=[] bound=1000 [47.9ms]
/-- (5,8,10): κ = 0.425000. -/
def sig5810 : BealFoundry.Signature := ⟨5, 8, 10, by omega, by omega, by omega⟩

theorem sig5810_hyperbolic : sig5810.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^10 up to 1000: no coprime solutions. -/
def cert_sig5810 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5810_hard : cert_sig5810.isHard = true := by native_decide

-- (5,8,11): κ=0.4159 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (5,8,11): κ = 0.415909. -/
def sig5811 : BealFoundry.Signature := ⟨5, 8, 11, by omega, by omega, by omega⟩

theorem sig5811_hyperbolic : sig5811.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^11 up to 1000: no coprime solutions. -/
def cert_sig5811 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5811_hard : cert_sig5811.isHard = true := by native_decide

-- (5,8,12): κ=0.4083 [hyperbolic] solutions=[] bound=1000 [48.0ms]
/-- (5,8,12): κ = 0.408333. -/
def sig5812 : BealFoundry.Signature := ⟨5, 8, 12, by omega, by omega, by omega⟩

theorem sig5812_hyperbolic : sig5812.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^12 up to 1000: no coprime solutions. -/
def cert_sig5812 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5812_hard : cert_sig5812.isHard = true := by native_decide

-- (5,8,13): κ=0.4019 [hyperbolic] solutions=[] bound=1000 [47.7ms]
/-- (5,8,13): κ = 0.401923. -/
def sig5813 : BealFoundry.Signature := ⟨5, 8, 13, by omega, by omega, by omega⟩

theorem sig5813_hyperbolic : sig5813.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^13 up to 1000: no coprime solutions. -/
def cert_sig5813 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5813_hard : cert_sig5813.isHard = true := by native_decide

-- (5,8,14): κ=0.3964 [hyperbolic] solutions=[] bound=1000 [47.3ms]
/-- (5,8,14): κ = 0.396429. -/
def sig5814 : BealFoundry.Signature := ⟨5, 8, 14, by omega, by omega, by omega⟩

theorem sig5814_hyperbolic : sig5814.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^14 up to 1000: no coprime solutions. -/
def cert_sig5814 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5814_hard : cert_sig5814.isHard = true := by native_decide

-- (5,8,15): κ=0.3917 [hyperbolic] solutions=[] bound=1000 [47.6ms]
/-- (5,8,15): κ = 0.391667. -/
def sig5815 : BealFoundry.Signature := ⟨5, 8, 15, by omega, by omega, by omega⟩

theorem sig5815_hyperbolic : sig5815.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^8 = c^15 up to 1000: no coprime solutions. -/
def cert_sig5815 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5815_hard : cert_sig5815.isHard = true := by native_decide

-- (5,9,9): κ=0.4222 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (5,9,9): κ = 0.422222. -/
def sig599 : BealFoundry.Signature := ⟨5, 9, 9, by omega, by omega, by omega⟩

theorem sig599_hyperbolic : sig599.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^9 = c^9 up to 1000: no coprime solutions. -/
def cert_sig599 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig599_hard : cert_sig599.isHard = true := by native_decide

-- (5,9,10): κ=0.4111 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (5,9,10): κ = 0.411111. -/
def sig5910 : BealFoundry.Signature := ⟨5, 9, 10, by omega, by omega, by omega⟩

theorem sig5910_hyperbolic : sig5910.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^9 = c^10 up to 1000: no coprime solutions. -/
def cert_sig5910 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5910_hard : cert_sig5910.isHard = true := by native_decide

-- (5,9,11): κ=0.4020 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (5,9,11): κ = 0.402020. -/
def sig5911 : BealFoundry.Signature := ⟨5, 9, 11, by omega, by omega, by omega⟩

theorem sig5911_hyperbolic : sig5911.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^9 = c^11 up to 1000: no coprime solutions. -/
def cert_sig5911 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5911_hard : cert_sig5911.isHard = true := by native_decide

-- (5,9,12): κ=0.3944 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (5,9,12): κ = 0.394444. -/
def sig5912 : BealFoundry.Signature := ⟨5, 9, 12, by omega, by omega, by omega⟩

theorem sig5912_hyperbolic : sig5912.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^9 = c^12 up to 1000: no coprime solutions. -/
def cert_sig5912 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5912_hard : cert_sig5912.isHard = true := by native_decide

-- (5,9,13): κ=0.3880 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (5,9,13): κ = 0.388034. -/
def sig5913 : BealFoundry.Signature := ⟨5, 9, 13, by omega, by omega, by omega⟩

theorem sig5913_hyperbolic : sig5913.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^9 = c^13 up to 1000: no coprime solutions. -/
def cert_sig5913 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5913_hard : cert_sig5913.isHard = true := by native_decide

-- (5,9,14): κ=0.3825 [hyperbolic] solutions=[] bound=1000 [47.9ms]
/-- (5,9,14): κ = 0.382540. -/
def sig5914 : BealFoundry.Signature := ⟨5, 9, 14, by omega, by omega, by omega⟩

theorem sig5914_hyperbolic : sig5914.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^9 = c^14 up to 1000: no coprime solutions. -/
def cert_sig5914 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5914_hard : cert_sig5914.isHard = true := by native_decide

-- (5,9,15): κ=0.3778 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (5,9,15): κ = 0.377778. -/
def sig5915 : BealFoundry.Signature := ⟨5, 9, 15, by omega, by omega, by omega⟩

theorem sig5915_hyperbolic : sig5915.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^9 = c^15 up to 1000: no coprime solutions. -/
def cert_sig5915 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig5915_hard : cert_sig5915.isHard = true := by native_decide

-- (5,10,10): κ=0.4000 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (5,10,10): κ = 0.400000. -/
def sig51010 : BealFoundry.Signature := ⟨5, 10, 10, by omega, by omega, by omega⟩

theorem sig51010_hyperbolic : sig51010.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig51010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51010_hard : cert_sig51010.isHard = true := by native_decide

-- (5,10,11): κ=0.3909 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (5,10,11): κ = 0.390909. -/
def sig51011 : BealFoundry.Signature := ⟨5, 10, 11, by omega, by omega, by omega⟩

theorem sig51011_hyperbolic : sig51011.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig51011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51011_hard : cert_sig51011.isHard = true := by native_decide

-- (5,10,12): κ=0.3833 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (5,10,12): κ = 0.383333. -/
def sig51012 : BealFoundry.Signature := ⟨5, 10, 12, by omega, by omega, by omega⟩

theorem sig51012_hyperbolic : sig51012.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig51012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51012_hard : cert_sig51012.isHard = true := by native_decide

-- (5,10,13): κ=0.3769 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (5,10,13): κ = 0.376923. -/
def sig51013 : BealFoundry.Signature := ⟨5, 10, 13, by omega, by omega, by omega⟩

theorem sig51013_hyperbolic : sig51013.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig51013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51013_hard : cert_sig51013.isHard = true := by native_decide

-- (5,10,14): κ=0.3714 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (5,10,14): κ = 0.371429. -/
def sig51014 : BealFoundry.Signature := ⟨5, 10, 14, by omega, by omega, by omega⟩

theorem sig51014_hyperbolic : sig51014.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig51014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51014_hard : cert_sig51014.isHard = true := by native_decide

-- (5,10,15): κ=0.3667 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (5,10,15): κ = 0.366667. -/
def sig51015 : BealFoundry.Signature := ⟨5, 10, 15, by omega, by omega, by omega⟩

theorem sig51015_hyperbolic : sig51015.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig51015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51015_hard : cert_sig51015.isHard = true := by native_decide

-- (5,11,11): κ=0.3818 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (5,11,11): κ = 0.381818. -/
def sig51111 : BealFoundry.Signature := ⟨5, 11, 11, by omega, by omega, by omega⟩

theorem sig51111_hyperbolic : sig51111.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig51111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51111_hard : cert_sig51111.isHard = true := by native_decide

-- (5,11,12): κ=0.3742 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (5,11,12): κ = 0.374242. -/
def sig51112 : BealFoundry.Signature := ⟨5, 11, 12, by omega, by omega, by omega⟩

theorem sig51112_hyperbolic : sig51112.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig51112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51112_hard : cert_sig51112.isHard = true := by native_decide

-- (5,11,13): κ=0.3678 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (5,11,13): κ = 0.367832. -/
def sig51113 : BealFoundry.Signature := ⟨5, 11, 13, by omega, by omega, by omega⟩

theorem sig51113_hyperbolic : sig51113.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig51113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51113_hard : cert_sig51113.isHard = true := by native_decide

-- (5,11,14): κ=0.3623 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (5,11,14): κ = 0.362338. -/
def sig51114 : BealFoundry.Signature := ⟨5, 11, 14, by omega, by omega, by omega⟩

theorem sig51114_hyperbolic : sig51114.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig51114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51114_hard : cert_sig51114.isHard = true := by native_decide

-- (5,11,15): κ=0.3576 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (5,11,15): κ = 0.357576. -/
def sig51115 : BealFoundry.Signature := ⟨5, 11, 15, by omega, by omega, by omega⟩

theorem sig51115_hyperbolic : sig51115.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig51115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51115_hard : cert_sig51115.isHard = true := by native_decide

-- (5,12,12): κ=0.3667 [hyperbolic] solutions=[] bound=1000 [50.0ms]
/-- (5,12,12): κ = 0.366667. -/
def sig51212 : BealFoundry.Signature := ⟨5, 12, 12, by omega, by omega, by omega⟩

theorem sig51212_hyperbolic : sig51212.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig51212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51212_hard : cert_sig51212.isHard = true := by native_decide

-- (5,12,13): κ=0.3603 [hyperbolic] solutions=[] bound=1000 [50.4ms]
/-- (5,12,13): κ = 0.360256. -/
def sig51213 : BealFoundry.Signature := ⟨5, 12, 13, by omega, by omega, by omega⟩

theorem sig51213_hyperbolic : sig51213.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig51213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51213_hard : cert_sig51213.isHard = true := by native_decide

-- (5,12,14): κ=0.3548 [hyperbolic] solutions=[] bound=1000 [50.9ms]
/-- (5,12,14): κ = 0.354762. -/
def sig51214 : BealFoundry.Signature := ⟨5, 12, 14, by omega, by omega, by omega⟩

theorem sig51214_hyperbolic : sig51214.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig51214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51214_hard : cert_sig51214.isHard = true := by native_decide

-- (5,12,15): κ=0.3500 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (5,12,15): κ = 0.350000. -/
def sig51215 : BealFoundry.Signature := ⟨5, 12, 15, by omega, by omega, by omega⟩

theorem sig51215_hyperbolic : sig51215.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig51215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51215_hard : cert_sig51215.isHard = true := by native_decide

-- (5,13,13): κ=0.3538 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (5,13,13): κ = 0.353846. -/
def sig51313 : BealFoundry.Signature := ⟨5, 13, 13, by omega, by omega, by omega⟩

theorem sig51313_hyperbolic : sig51313.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig51313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51313_hard : cert_sig51313.isHard = true := by native_decide

-- (5,13,14): κ=0.3484 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (5,13,14): κ = 0.348352. -/
def sig51314 : BealFoundry.Signature := ⟨5, 13, 14, by omega, by omega, by omega⟩

theorem sig51314_hyperbolic : sig51314.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig51314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51314_hard : cert_sig51314.isHard = true := by native_decide

-- (5,13,15): κ=0.3436 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (5,13,15): κ = 0.343590. -/
def sig51315 : BealFoundry.Signature := ⟨5, 13, 15, by omega, by omega, by omega⟩

theorem sig51315_hyperbolic : sig51315.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig51315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51315_hard : cert_sig51315.isHard = true := by native_decide

-- (5,14,14): κ=0.3429 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (5,14,14): κ = 0.342857. -/
def sig51414 : BealFoundry.Signature := ⟨5, 14, 14, by omega, by omega, by omega⟩

theorem sig51414_hyperbolic : sig51414.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig51414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51414_hard : cert_sig51414.isHard = true := by native_decide

-- (5,14,15): κ=0.3381 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (5,14,15): κ = 0.338095. -/
def sig51415 : BealFoundry.Signature := ⟨5, 14, 15, by omega, by omega, by omega⟩

theorem sig51415_hyperbolic : sig51415.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig51415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51415_hard : cert_sig51415.isHard = true := by native_decide

-- (5,15,15): κ=0.3333 [hyperbolic] solutions=[] bound=1000 [50.1ms]
/-- (5,15,15): κ = 0.333333. -/
def sig51515 : BealFoundry.Signature := ⟨5, 15, 15, by omega, by omega, by omega⟩

theorem sig51515_hyperbolic : sig51515.regime = .hyperbolic := by
  native_decide

/-- Searched a^5 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig51515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig51515_hard : cert_sig51515.isHard = true := by native_decide

-- (6,6,6): κ=0.5000 [hyperbolic] solutions=[] bound=1000 [46.8ms]
/-- (6,6,6): κ = 0.500000. -/
def sig666 : BealFoundry.Signature := ⟨6, 6, 6, by omega, by omega, by omega⟩

theorem sig666_hyperbolic : sig666.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^6 up to 1000: no coprime solutions. -/
def cert_sig666 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig666_hard : cert_sig666.isHard = true := by native_decide

-- (6,6,7): κ=0.4762 [hyperbolic] solutions=[] bound=1000 [47.3ms]
/-- (6,6,7): κ = 0.476190. -/
def sig667 : BealFoundry.Signature := ⟨6, 6, 7, by omega, by omega, by omega⟩

theorem sig667_hyperbolic : sig667.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^7 up to 1000: no coprime solutions. -/
def cert_sig667 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig667_hard : cert_sig667.isHard = true := by native_decide

-- (6,6,8): κ=0.4583 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (6,6,8): κ = 0.458333. -/
def sig668 : BealFoundry.Signature := ⟨6, 6, 8, by omega, by omega, by omega⟩

theorem sig668_hyperbolic : sig668.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^8 up to 1000: no coprime solutions. -/
def cert_sig668 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig668_hard : cert_sig668.isHard = true := by native_decide

-- (6,6,9): κ=0.4444 [hyperbolic] solutions=[] bound=1000 [47.2ms]
/-- (6,6,9): κ = 0.444444. -/
def sig669 : BealFoundry.Signature := ⟨6, 6, 9, by omega, by omega, by omega⟩

theorem sig669_hyperbolic : sig669.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^9 up to 1000: no coprime solutions. -/
def cert_sig669 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig669_hard : cert_sig669.isHard = true := by native_decide

-- (6,6,10): κ=0.4333 [hyperbolic] solutions=[] bound=1000 [46.0ms]
/-- (6,6,10): κ = 0.433333. -/
def sig6610 : BealFoundry.Signature := ⟨6, 6, 10, by omega, by omega, by omega⟩

theorem sig6610_hyperbolic : sig6610.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^10 up to 1000: no coprime solutions. -/
def cert_sig6610 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6610_hard : cert_sig6610.isHard = true := by native_decide

-- (6,6,11): κ=0.4242 [hyperbolic] solutions=[] bound=1000 [46.2ms]
/-- (6,6,11): κ = 0.424242. -/
def sig6611 : BealFoundry.Signature := ⟨6, 6, 11, by omega, by omega, by omega⟩

theorem sig6611_hyperbolic : sig6611.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^11 up to 1000: no coprime solutions. -/
def cert_sig6611 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6611_hard : cert_sig6611.isHard = true := by native_decide

-- (6,6,12): κ=0.4167 [hyperbolic] solutions=[] bound=1000 [46.9ms]
/-- (6,6,12): κ = 0.416667. -/
def sig6612 : BealFoundry.Signature := ⟨6, 6, 12, by omega, by omega, by omega⟩

theorem sig6612_hyperbolic : sig6612.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^12 up to 1000: no coprime solutions. -/
def cert_sig6612 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6612_hard : cert_sig6612.isHard = true := by native_decide

-- (6,6,13): κ=0.4103 [hyperbolic] solutions=[] bound=1000 [46.0ms]
/-- (6,6,13): κ = 0.410256. -/
def sig6613 : BealFoundry.Signature := ⟨6, 6, 13, by omega, by omega, by omega⟩

theorem sig6613_hyperbolic : sig6613.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^13 up to 1000: no coprime solutions. -/
def cert_sig6613 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6613_hard : cert_sig6613.isHard = true := by native_decide

-- (6,6,14): κ=0.4048 [hyperbolic] solutions=[] bound=1000 [46.5ms]
/-- (6,6,14): κ = 0.404762. -/
def sig6614 : BealFoundry.Signature := ⟨6, 6, 14, by omega, by omega, by omega⟩

theorem sig6614_hyperbolic : sig6614.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^14 up to 1000: no coprime solutions. -/
def cert_sig6614 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6614_hard : cert_sig6614.isHard = true := by native_decide

-- (6,6,15): κ=0.4000 [hyperbolic] solutions=[] bound=1000 [46.1ms]
/-- (6,6,15): κ = 0.400000. -/
def sig6615 : BealFoundry.Signature := ⟨6, 6, 15, by omega, by omega, by omega⟩

theorem sig6615_hyperbolic : sig6615.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^6 = c^15 up to 1000: no coprime solutions. -/
def cert_sig6615 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6615_hard : cert_sig6615.isHard = true := by native_decide

-- (6,7,7): κ=0.4524 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (6,7,7): κ = 0.452381. -/
def sig677 : BealFoundry.Signature := ⟨6, 7, 7, by omega, by omega, by omega⟩

theorem sig677_hyperbolic : sig677.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^7 up to 1000: no coprime solutions. -/
def cert_sig677 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig677_hard : cert_sig677.isHard = true := by native_decide

-- (6,7,8): κ=0.4345 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (6,7,8): κ = 0.434524. -/
def sig678 : BealFoundry.Signature := ⟨6, 7, 8, by omega, by omega, by omega⟩

theorem sig678_hyperbolic : sig678.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^8 up to 1000: no coprime solutions. -/
def cert_sig678 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig678_hard : cert_sig678.isHard = true := by native_decide

-- (6,7,9): κ=0.4206 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (6,7,9): κ = 0.420635. -/
def sig679 : BealFoundry.Signature := ⟨6, 7, 9, by omega, by omega, by omega⟩

theorem sig679_hyperbolic : sig679.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^9 up to 1000: no coprime solutions. -/
def cert_sig679 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig679_hard : cert_sig679.isHard = true := by native_decide

-- (6,7,10): κ=0.4095 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (6,7,10): κ = 0.409524. -/
def sig6710 : BealFoundry.Signature := ⟨6, 7, 10, by omega, by omega, by omega⟩

theorem sig6710_hyperbolic : sig6710.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^10 up to 1000: no coprime solutions. -/
def cert_sig6710 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6710_hard : cert_sig6710.isHard = true := by native_decide

-- (6,7,11): κ=0.4004 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (6,7,11): κ = 0.400433. -/
def sig6711 : BealFoundry.Signature := ⟨6, 7, 11, by omega, by omega, by omega⟩

theorem sig6711_hyperbolic : sig6711.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^11 up to 1000: no coprime solutions. -/
def cert_sig6711 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6711_hard : cert_sig6711.isHard = true := by native_decide

-- (6,7,12): κ=0.3929 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (6,7,12): κ = 0.392857. -/
def sig6712 : BealFoundry.Signature := ⟨6, 7, 12, by omega, by omega, by omega⟩

theorem sig6712_hyperbolic : sig6712.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^12 up to 1000: no coprime solutions. -/
def cert_sig6712 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6712_hard : cert_sig6712.isHard = true := by native_decide

-- (6,7,13): κ=0.3864 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (6,7,13): κ = 0.386447. -/
def sig6713 : BealFoundry.Signature := ⟨6, 7, 13, by omega, by omega, by omega⟩

theorem sig6713_hyperbolic : sig6713.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^13 up to 1000: no coprime solutions. -/
def cert_sig6713 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6713_hard : cert_sig6713.isHard = true := by native_decide

-- (6,7,14): κ=0.3810 [hyperbolic] solutions=[] bound=1000 [48.0ms]
/-- (6,7,14): κ = 0.380952. -/
def sig6714 : BealFoundry.Signature := ⟨6, 7, 14, by omega, by omega, by omega⟩

theorem sig6714_hyperbolic : sig6714.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^14 up to 1000: no coprime solutions. -/
def cert_sig6714 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6714_hard : cert_sig6714.isHard = true := by native_decide

-- (6,7,15): κ=0.3762 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (6,7,15): κ = 0.376190. -/
def sig6715 : BealFoundry.Signature := ⟨6, 7, 15, by omega, by omega, by omega⟩

theorem sig6715_hyperbolic : sig6715.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^7 = c^15 up to 1000: no coprime solutions. -/
def cert_sig6715 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6715_hard : cert_sig6715.isHard = true := by native_decide

-- (6,8,8): κ=0.4167 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (6,8,8): κ = 0.416667. -/
def sig688 : BealFoundry.Signature := ⟨6, 8, 8, by omega, by omega, by omega⟩

theorem sig688_hyperbolic : sig688.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^8 up to 1000: no coprime solutions. -/
def cert_sig688 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig688_hard : cert_sig688.isHard = true := by native_decide

-- (6,8,9): κ=0.4028 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (6,8,9): κ = 0.402778. -/
def sig689 : BealFoundry.Signature := ⟨6, 8, 9, by omega, by omega, by omega⟩

theorem sig689_hyperbolic : sig689.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^9 up to 1000: no coprime solutions. -/
def cert_sig689 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig689_hard : cert_sig689.isHard = true := by native_decide

-- (6,8,10): κ=0.3917 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (6,8,10): κ = 0.391667. -/
def sig6810 : BealFoundry.Signature := ⟨6, 8, 10, by omega, by omega, by omega⟩

theorem sig6810_hyperbolic : sig6810.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^10 up to 1000: no coprime solutions. -/
def cert_sig6810 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6810_hard : cert_sig6810.isHard = true := by native_decide

-- (6,8,11): κ=0.3826 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (6,8,11): κ = 0.382576. -/
def sig6811 : BealFoundry.Signature := ⟨6, 8, 11, by omega, by omega, by omega⟩

theorem sig6811_hyperbolic : sig6811.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^11 up to 1000: no coprime solutions. -/
def cert_sig6811 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6811_hard : cert_sig6811.isHard = true := by native_decide

-- (6,8,12): κ=0.3750 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (6,8,12): κ = 0.375000. -/
def sig6812 : BealFoundry.Signature := ⟨6, 8, 12, by omega, by omega, by omega⟩

theorem sig6812_hyperbolic : sig6812.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^12 up to 1000: no coprime solutions. -/
def cert_sig6812 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6812_hard : cert_sig6812.isHard = true := by native_decide

-- (6,8,13): κ=0.3686 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (6,8,13): κ = 0.368590. -/
def sig6813 : BealFoundry.Signature := ⟨6, 8, 13, by omega, by omega, by omega⟩

theorem sig6813_hyperbolic : sig6813.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^13 up to 1000: no coprime solutions. -/
def cert_sig6813 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6813_hard : cert_sig6813.isHard = true := by native_decide

-- (6,8,14): κ=0.3631 [hyperbolic] solutions=[] bound=1000 [48.0ms]
/-- (6,8,14): κ = 0.363095. -/
def sig6814 : BealFoundry.Signature := ⟨6, 8, 14, by omega, by omega, by omega⟩

theorem sig6814_hyperbolic : sig6814.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^14 up to 1000: no coprime solutions. -/
def cert_sig6814 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6814_hard : cert_sig6814.isHard = true := by native_decide

-- (6,8,15): κ=0.3583 [hyperbolic] solutions=[] bound=1000 [47.9ms]
/-- (6,8,15): κ = 0.358333. -/
def sig6815 : BealFoundry.Signature := ⟨6, 8, 15, by omega, by omega, by omega⟩

theorem sig6815_hyperbolic : sig6815.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^8 = c^15 up to 1000: no coprime solutions. -/
def cert_sig6815 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6815_hard : cert_sig6815.isHard = true := by native_decide

-- (6,9,9): κ=0.3889 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (6,9,9): κ = 0.388889. -/
def sig699 : BealFoundry.Signature := ⟨6, 9, 9, by omega, by omega, by omega⟩

theorem sig699_hyperbolic : sig699.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^9 = c^9 up to 1000: no coprime solutions. -/
def cert_sig699 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig699_hard : cert_sig699.isHard = true := by native_decide

-- (6,9,10): κ=0.3778 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (6,9,10): κ = 0.377778. -/
def sig6910 : BealFoundry.Signature := ⟨6, 9, 10, by omega, by omega, by omega⟩

theorem sig6910_hyperbolic : sig6910.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^9 = c^10 up to 1000: no coprime solutions. -/
def cert_sig6910 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6910_hard : cert_sig6910.isHard = true := by native_decide

-- (6,9,11): κ=0.3687 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (6,9,11): κ = 0.368687. -/
def sig6911 : BealFoundry.Signature := ⟨6, 9, 11, by omega, by omega, by omega⟩

theorem sig6911_hyperbolic : sig6911.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^9 = c^11 up to 1000: no coprime solutions. -/
def cert_sig6911 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6911_hard : cert_sig6911.isHard = true := by native_decide

-- (6,9,12): κ=0.3611 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (6,9,12): κ = 0.361111. -/
def sig6912 : BealFoundry.Signature := ⟨6, 9, 12, by omega, by omega, by omega⟩

theorem sig6912_hyperbolic : sig6912.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^9 = c^12 up to 1000: no coprime solutions. -/
def cert_sig6912 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6912_hard : cert_sig6912.isHard = true := by native_decide

-- (6,9,13): κ=0.3547 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (6,9,13): κ = 0.354701. -/
def sig6913 : BealFoundry.Signature := ⟨6, 9, 13, by omega, by omega, by omega⟩

theorem sig6913_hyperbolic : sig6913.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^9 = c^13 up to 1000: no coprime solutions. -/
def cert_sig6913 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6913_hard : cert_sig6913.isHard = true := by native_decide

-- (6,9,14): κ=0.3492 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (6,9,14): κ = 0.349206. -/
def sig6914 : BealFoundry.Signature := ⟨6, 9, 14, by omega, by omega, by omega⟩

theorem sig6914_hyperbolic : sig6914.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^9 = c^14 up to 1000: no coprime solutions. -/
def cert_sig6914 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6914_hard : cert_sig6914.isHard = true := by native_decide

-- (6,9,15): κ=0.3444 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (6,9,15): κ = 0.344444. -/
def sig6915 : BealFoundry.Signature := ⟨6, 9, 15, by omega, by omega, by omega⟩

theorem sig6915_hyperbolic : sig6915.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^9 = c^15 up to 1000: no coprime solutions. -/
def cert_sig6915 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig6915_hard : cert_sig6915.isHard = true := by native_decide

-- (6,10,10): κ=0.3667 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (6,10,10): κ = 0.366667. -/
def sig61010 : BealFoundry.Signature := ⟨6, 10, 10, by omega, by omega, by omega⟩

theorem sig61010_hyperbolic : sig61010.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig61010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61010_hard : cert_sig61010.isHard = true := by native_decide

-- (6,10,11): κ=0.3576 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (6,10,11): κ = 0.357576. -/
def sig61011 : BealFoundry.Signature := ⟨6, 10, 11, by omega, by omega, by omega⟩

theorem sig61011_hyperbolic : sig61011.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig61011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61011_hard : cert_sig61011.isHard = true := by native_decide

-- (6,10,12): κ=0.3500 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (6,10,12): κ = 0.350000. -/
def sig61012 : BealFoundry.Signature := ⟨6, 10, 12, by omega, by omega, by omega⟩

theorem sig61012_hyperbolic : sig61012.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig61012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61012_hard : cert_sig61012.isHard = true := by native_decide

-- (6,10,13): κ=0.3436 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (6,10,13): κ = 0.343590. -/
def sig61013 : BealFoundry.Signature := ⟨6, 10, 13, by omega, by omega, by omega⟩

theorem sig61013_hyperbolic : sig61013.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig61013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61013_hard : cert_sig61013.isHard = true := by native_decide

-- (6,10,14): κ=0.3381 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (6,10,14): κ = 0.338095. -/
def sig61014 : BealFoundry.Signature := ⟨6, 10, 14, by omega, by omega, by omega⟩

theorem sig61014_hyperbolic : sig61014.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig61014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61014_hard : cert_sig61014.isHard = true := by native_decide

-- (6,10,15): κ=0.3333 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (6,10,15): κ = 0.333333. -/
def sig61015 : BealFoundry.Signature := ⟨6, 10, 15, by omega, by omega, by omega⟩

theorem sig61015_hyperbolic : sig61015.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig61015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61015_hard : cert_sig61015.isHard = true := by native_decide

-- (6,11,11): κ=0.3485 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (6,11,11): κ = 0.348485. -/
def sig61111 : BealFoundry.Signature := ⟨6, 11, 11, by omega, by omega, by omega⟩

theorem sig61111_hyperbolic : sig61111.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig61111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61111_hard : cert_sig61111.isHard = true := by native_decide

-- (6,11,12): κ=0.3409 [hyperbolic] solutions=[] bound=1000 [53.0ms]
/-- (6,11,12): κ = 0.340909. -/
def sig61112 : BealFoundry.Signature := ⟨6, 11, 12, by omega, by omega, by omega⟩

theorem sig61112_hyperbolic : sig61112.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig61112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61112_hard : cert_sig61112.isHard = true := by native_decide

-- (6,11,13): κ=0.3345 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (6,11,13): κ = 0.334499. -/
def sig61113 : BealFoundry.Signature := ⟨6, 11, 13, by omega, by omega, by omega⟩

theorem sig61113_hyperbolic : sig61113.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig61113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61113_hard : cert_sig61113.isHard = true := by native_decide

-- (6,11,14): κ=0.3290 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (6,11,14): κ = 0.329004. -/
def sig61114 : BealFoundry.Signature := ⟨6, 11, 14, by omega, by omega, by omega⟩

theorem sig61114_hyperbolic : sig61114.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig61114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61114_hard : cert_sig61114.isHard = true := by native_decide

-- (6,11,15): κ=0.3242 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (6,11,15): κ = 0.324242. -/
def sig61115 : BealFoundry.Signature := ⟨6, 11, 15, by omega, by omega, by omega⟩

theorem sig61115_hyperbolic : sig61115.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig61115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61115_hard : cert_sig61115.isHard = true := by native_decide

-- (6,12,12): κ=0.3333 [hyperbolic] solutions=[] bound=1000 [50.7ms]
/-- (6,12,12): κ = 0.333333. -/
def sig61212 : BealFoundry.Signature := ⟨6, 12, 12, by omega, by omega, by omega⟩

theorem sig61212_hyperbolic : sig61212.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig61212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61212_hard : cert_sig61212.isHard = true := by native_decide

-- (6,12,13): κ=0.3269 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (6,12,13): κ = 0.326923. -/
def sig61213 : BealFoundry.Signature := ⟨6, 12, 13, by omega, by omega, by omega⟩

theorem sig61213_hyperbolic : sig61213.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig61213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61213_hard : cert_sig61213.isHard = true := by native_decide

-- (6,12,14): κ=0.3214 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (6,12,14): κ = 0.321429. -/
def sig61214 : BealFoundry.Signature := ⟨6, 12, 14, by omega, by omega, by omega⟩

theorem sig61214_hyperbolic : sig61214.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig61214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61214_hard : cert_sig61214.isHard = true := by native_decide

-- (6,12,15): κ=0.3167 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (6,12,15): κ = 0.316667. -/
def sig61215 : BealFoundry.Signature := ⟨6, 12, 15, by omega, by omega, by omega⟩

theorem sig61215_hyperbolic : sig61215.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig61215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61215_hard : cert_sig61215.isHard = true := by native_decide

-- (6,13,13): κ=0.3205 [hyperbolic] solutions=[] bound=1000 [50.4ms]
/-- (6,13,13): κ = 0.320513. -/
def sig61313 : BealFoundry.Signature := ⟨6, 13, 13, by omega, by omega, by omega⟩

theorem sig61313_hyperbolic : sig61313.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig61313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61313_hard : cert_sig61313.isHard = true := by native_decide

-- (6,13,14): κ=0.3150 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (6,13,14): κ = 0.315018. -/
def sig61314 : BealFoundry.Signature := ⟨6, 13, 14, by omega, by omega, by omega⟩

theorem sig61314_hyperbolic : sig61314.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig61314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61314_hard : cert_sig61314.isHard = true := by native_decide

-- (6,13,15): κ=0.3103 [hyperbolic] solutions=[] bound=1000 [50.1ms]
/-- (6,13,15): κ = 0.310256. -/
def sig61315 : BealFoundry.Signature := ⟨6, 13, 15, by omega, by omega, by omega⟩

theorem sig61315_hyperbolic : sig61315.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig61315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61315_hard : cert_sig61315.isHard = true := by native_decide

-- (6,14,14): κ=0.3095 [hyperbolic] solutions=[] bound=1000 [52.3ms]
/-- (6,14,14): κ = 0.309524. -/
def sig61414 : BealFoundry.Signature := ⟨6, 14, 14, by omega, by omega, by omega⟩

theorem sig61414_hyperbolic : sig61414.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig61414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61414_hard : cert_sig61414.isHard = true := by native_decide

-- (6,14,15): κ=0.3048 [hyperbolic] solutions=[] bound=1000 [50.0ms]
/-- (6,14,15): κ = 0.304762. -/
def sig61415 : BealFoundry.Signature := ⟨6, 14, 15, by omega, by omega, by omega⟩

theorem sig61415_hyperbolic : sig61415.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig61415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61415_hard : cert_sig61415.isHard = true := by native_decide

-- (6,15,15): κ=0.3000 [hyperbolic] solutions=[] bound=1000 [50.4ms]
/-- (6,15,15): κ = 0.300000. -/
def sig61515 : BealFoundry.Signature := ⟨6, 15, 15, by omega, by omega, by omega⟩

theorem sig61515_hyperbolic : sig61515.regime = .hyperbolic := by
  native_decide

/-- Searched a^6 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig61515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig61515_hard : cert_sig61515.isHard = true := by native_decide

-- (7,7,7): κ=0.4286 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (7,7,7): κ = 0.428571. -/
def sig777 : BealFoundry.Signature := ⟨7, 7, 7, by omega, by omega, by omega⟩

theorem sig777_hyperbolic : sig777.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^7 up to 1000: no coprime solutions. -/
def cert_sig777 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig777_hard : cert_sig777.isHard = true := by native_decide

-- (7,7,8): κ=0.4107 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (7,7,8): κ = 0.410714. -/
def sig778 : BealFoundry.Signature := ⟨7, 7, 8, by omega, by omega, by omega⟩

theorem sig778_hyperbolic : sig778.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^8 up to 1000: no coprime solutions. -/
def cert_sig778 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig778_hard : cert_sig778.isHard = true := by native_decide

-- (7,7,9): κ=0.3968 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (7,7,9): κ = 0.396825. -/
def sig779 : BealFoundry.Signature := ⟨7, 7, 9, by omega, by omega, by omega⟩

theorem sig779_hyperbolic : sig779.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^9 up to 1000: no coprime solutions. -/
def cert_sig779 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig779_hard : cert_sig779.isHard = true := by native_decide

-- (7,7,10): κ=0.3857 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (7,7,10): κ = 0.385714. -/
def sig7710 : BealFoundry.Signature := ⟨7, 7, 10, by omega, by omega, by omega⟩

theorem sig7710_hyperbolic : sig7710.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^10 up to 1000: no coprime solutions. -/
def cert_sig7710 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7710_hard : cert_sig7710.isHard = true := by native_decide

-- (7,7,11): κ=0.3766 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (7,7,11): κ = 0.376623. -/
def sig7711 : BealFoundry.Signature := ⟨7, 7, 11, by omega, by omega, by omega⟩

theorem sig7711_hyperbolic : sig7711.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^11 up to 1000: no coprime solutions. -/
def cert_sig7711 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7711_hard : cert_sig7711.isHard = true := by native_decide

-- (7,7,12): κ=0.3690 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (7,7,12): κ = 0.369048. -/
def sig7712 : BealFoundry.Signature := ⟨7, 7, 12, by omega, by omega, by omega⟩

theorem sig7712_hyperbolic : sig7712.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^12 up to 1000: no coprime solutions. -/
def cert_sig7712 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7712_hard : cert_sig7712.isHard = true := by native_decide

-- (7,7,13): κ=0.3626 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (7,7,13): κ = 0.362637. -/
def sig7713 : BealFoundry.Signature := ⟨7, 7, 13, by omega, by omega, by omega⟩

theorem sig7713_hyperbolic : sig7713.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^13 up to 1000: no coprime solutions. -/
def cert_sig7713 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7713_hard : cert_sig7713.isHard = true := by native_decide

-- (7,7,14): κ=0.3571 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (7,7,14): κ = 0.357143. -/
def sig7714 : BealFoundry.Signature := ⟨7, 7, 14, by omega, by omega, by omega⟩

theorem sig7714_hyperbolic : sig7714.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^14 up to 1000: no coprime solutions. -/
def cert_sig7714 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7714_hard : cert_sig7714.isHard = true := by native_decide

-- (7,7,15): κ=0.3524 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (7,7,15): κ = 0.352381. -/
def sig7715 : BealFoundry.Signature := ⟨7, 7, 15, by omega, by omega, by omega⟩

theorem sig7715_hyperbolic : sig7715.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^7 = c^15 up to 1000: no coprime solutions. -/
def cert_sig7715 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7715_hard : cert_sig7715.isHard = true := by native_decide

-- (7,8,8): κ=0.3929 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (7,8,8): κ = 0.392857. -/
def sig788 : BealFoundry.Signature := ⟨7, 8, 8, by omega, by omega, by omega⟩

theorem sig788_hyperbolic : sig788.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^8 up to 1000: no coprime solutions. -/
def cert_sig788 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig788_hard : cert_sig788.isHard = true := by native_decide

-- (7,8,9): κ=0.3790 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (7,8,9): κ = 0.378968. -/
def sig789 : BealFoundry.Signature := ⟨7, 8, 9, by omega, by omega, by omega⟩

theorem sig789_hyperbolic : sig789.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^9 up to 1000: no coprime solutions. -/
def cert_sig789 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig789_hard : cert_sig789.isHard = true := by native_decide

-- (7,8,10): κ=0.3679 [hyperbolic] solutions=[] bound=1000 [48.0ms]
/-- (7,8,10): κ = 0.367857. -/
def sig7810 : BealFoundry.Signature := ⟨7, 8, 10, by omega, by omega, by omega⟩

theorem sig7810_hyperbolic : sig7810.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^10 up to 1000: no coprime solutions. -/
def cert_sig7810 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7810_hard : cert_sig7810.isHard = true := by native_decide

-- (7,8,11): κ=0.3588 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (7,8,11): κ = 0.358766. -/
def sig7811 : BealFoundry.Signature := ⟨7, 8, 11, by omega, by omega, by omega⟩

theorem sig7811_hyperbolic : sig7811.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^11 up to 1000: no coprime solutions. -/
def cert_sig7811 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7811_hard : cert_sig7811.isHard = true := by native_decide

-- (7,8,12): κ=0.3512 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (7,8,12): κ = 0.351190. -/
def sig7812 : BealFoundry.Signature := ⟨7, 8, 12, by omega, by omega, by omega⟩

theorem sig7812_hyperbolic : sig7812.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^12 up to 1000: no coprime solutions. -/
def cert_sig7812 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7812_hard : cert_sig7812.isHard = true := by native_decide

-- (7,8,13): κ=0.3448 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (7,8,13): κ = 0.344780. -/
def sig7813 : BealFoundry.Signature := ⟨7, 8, 13, by omega, by omega, by omega⟩

theorem sig7813_hyperbolic : sig7813.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^13 up to 1000: no coprime solutions. -/
def cert_sig7813 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7813_hard : cert_sig7813.isHard = true := by native_decide

-- (7,8,14): κ=0.3393 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (7,8,14): κ = 0.339286. -/
def sig7814 : BealFoundry.Signature := ⟨7, 8, 14, by omega, by omega, by omega⟩

theorem sig7814_hyperbolic : sig7814.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^14 up to 1000: no coprime solutions. -/
def cert_sig7814 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7814_hard : cert_sig7814.isHard = true := by native_decide

-- (7,8,15): κ=0.3345 [hyperbolic] solutions=[] bound=1000 [48.1ms]
/-- (7,8,15): κ = 0.334524. -/
def sig7815 : BealFoundry.Signature := ⟨7, 8, 15, by omega, by omega, by omega⟩

theorem sig7815_hyperbolic : sig7815.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^8 = c^15 up to 1000: no coprime solutions. -/
def cert_sig7815 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7815_hard : cert_sig7815.isHard = true := by native_decide

-- (7,9,9): κ=0.3651 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (7,9,9): κ = 0.365079. -/
def sig799 : BealFoundry.Signature := ⟨7, 9, 9, by omega, by omega, by omega⟩

theorem sig799_hyperbolic : sig799.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^9 = c^9 up to 1000: no coprime solutions. -/
def cert_sig799 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig799_hard : cert_sig799.isHard = true := by native_decide

-- (7,9,10): κ=0.3540 [hyperbolic] solutions=[] bound=1000 [48.0ms]
/-- (7,9,10): κ = 0.353968. -/
def sig7910 : BealFoundry.Signature := ⟨7, 9, 10, by omega, by omega, by omega⟩

theorem sig7910_hyperbolic : sig7910.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^9 = c^10 up to 1000: no coprime solutions. -/
def cert_sig7910 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7910_hard : cert_sig7910.isHard = true := by native_decide

-- (7,9,11): κ=0.3449 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (7,9,11): κ = 0.344877. -/
def sig7911 : BealFoundry.Signature := ⟨7, 9, 11, by omega, by omega, by omega⟩

theorem sig7911_hyperbolic : sig7911.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^9 = c^11 up to 1000: no coprime solutions. -/
def cert_sig7911 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7911_hard : cert_sig7911.isHard = true := by native_decide

-- (7,9,12): κ=0.3373 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (7,9,12): κ = 0.337302. -/
def sig7912 : BealFoundry.Signature := ⟨7, 9, 12, by omega, by omega, by omega⟩

theorem sig7912_hyperbolic : sig7912.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^9 = c^12 up to 1000: no coprime solutions. -/
def cert_sig7912 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7912_hard : cert_sig7912.isHard = true := by native_decide

-- (7,9,13): κ=0.3309 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (7,9,13): κ = 0.330891. -/
def sig7913 : BealFoundry.Signature := ⟨7, 9, 13, by omega, by omega, by omega⟩

theorem sig7913_hyperbolic : sig7913.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^9 = c^13 up to 1000: no coprime solutions. -/
def cert_sig7913 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7913_hard : cert_sig7913.isHard = true := by native_decide

-- (7,9,14): κ=0.3254 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (7,9,14): κ = 0.325397. -/
def sig7914 : BealFoundry.Signature := ⟨7, 9, 14, by omega, by omega, by omega⟩

theorem sig7914_hyperbolic : sig7914.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^9 = c^14 up to 1000: no coprime solutions. -/
def cert_sig7914 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7914_hard : cert_sig7914.isHard = true := by native_decide

-- (7,9,15): κ=0.3206 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (7,9,15): κ = 0.320635. -/
def sig7915 : BealFoundry.Signature := ⟨7, 9, 15, by omega, by omega, by omega⟩

theorem sig7915_hyperbolic : sig7915.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^9 = c^15 up to 1000: no coprime solutions. -/
def cert_sig7915 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig7915_hard : cert_sig7915.isHard = true := by native_decide

-- (7,10,10): κ=0.3429 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (7,10,10): κ = 0.342857. -/
def sig71010 : BealFoundry.Signature := ⟨7, 10, 10, by omega, by omega, by omega⟩

theorem sig71010_hyperbolic : sig71010.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig71010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71010_hard : cert_sig71010.isHard = true := by native_decide

-- (7,10,11): κ=0.3338 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (7,10,11): κ = 0.333766. -/
def sig71011 : BealFoundry.Signature := ⟨7, 10, 11, by omega, by omega, by omega⟩

theorem sig71011_hyperbolic : sig71011.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig71011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71011_hard : cert_sig71011.isHard = true := by native_decide

-- (7,10,12): κ=0.3262 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (7,10,12): κ = 0.326190. -/
def sig71012 : BealFoundry.Signature := ⟨7, 10, 12, by omega, by omega, by omega⟩

theorem sig71012_hyperbolic : sig71012.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig71012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71012_hard : cert_sig71012.isHard = true := by native_decide

-- (7,10,13): κ=0.3198 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (7,10,13): κ = 0.319780. -/
def sig71013 : BealFoundry.Signature := ⟨7, 10, 13, by omega, by omega, by omega⟩

theorem sig71013_hyperbolic : sig71013.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig71013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71013_hard : cert_sig71013.isHard = true := by native_decide

-- (7,10,14): κ=0.3143 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (7,10,14): κ = 0.314286. -/
def sig71014 : BealFoundry.Signature := ⟨7, 10, 14, by omega, by omega, by omega⟩

theorem sig71014_hyperbolic : sig71014.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig71014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71014_hard : cert_sig71014.isHard = true := by native_decide

-- (7,10,15): κ=0.3095 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (7,10,15): κ = 0.309524. -/
def sig71015 : BealFoundry.Signature := ⟨7, 10, 15, by omega, by omega, by omega⟩

theorem sig71015_hyperbolic : sig71015.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig71015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71015_hard : cert_sig71015.isHard = true := by native_decide

-- (7,11,11): κ=0.3247 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (7,11,11): κ = 0.324675. -/
def sig71111 : BealFoundry.Signature := ⟨7, 11, 11, by omega, by omega, by omega⟩

theorem sig71111_hyperbolic : sig71111.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig71111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71111_hard : cert_sig71111.isHard = true := by native_decide

-- (7,11,12): κ=0.3171 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (7,11,12): κ = 0.317100. -/
def sig71112 : BealFoundry.Signature := ⟨7, 11, 12, by omega, by omega, by omega⟩

theorem sig71112_hyperbolic : sig71112.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig71112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71112_hard : cert_sig71112.isHard = true := by native_decide

-- (7,11,13): κ=0.3107 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (7,11,13): κ = 0.310689. -/
def sig71113 : BealFoundry.Signature := ⟨7, 11, 13, by omega, by omega, by omega⟩

theorem sig71113_hyperbolic : sig71113.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig71113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71113_hard : cert_sig71113.isHard = true := by native_decide

-- (7,11,14): κ=0.3052 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (7,11,14): κ = 0.305195. -/
def sig71114 : BealFoundry.Signature := ⟨7, 11, 14, by omega, by omega, by omega⟩

theorem sig71114_hyperbolic : sig71114.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig71114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71114_hard : cert_sig71114.isHard = true := by native_decide

-- (7,11,15): κ=0.3004 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (7,11,15): κ = 0.300433. -/
def sig71115 : BealFoundry.Signature := ⟨7, 11, 15, by omega, by omega, by omega⟩

theorem sig71115_hyperbolic : sig71115.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig71115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71115_hard : cert_sig71115.isHard = true := by native_decide

-- (7,12,12): κ=0.3095 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (7,12,12): κ = 0.309524. -/
def sig71212 : BealFoundry.Signature := ⟨7, 12, 12, by omega, by omega, by omega⟩

theorem sig71212_hyperbolic : sig71212.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig71212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71212_hard : cert_sig71212.isHard = true := by native_decide

-- (7,12,13): κ=0.3031 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (7,12,13): κ = 0.303114. -/
def sig71213 : BealFoundry.Signature := ⟨7, 12, 13, by omega, by omega, by omega⟩

theorem sig71213_hyperbolic : sig71213.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig71213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71213_hard : cert_sig71213.isHard = true := by native_decide

-- (7,12,14): κ=0.2976 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (7,12,14): κ = 0.297619. -/
def sig71214 : BealFoundry.Signature := ⟨7, 12, 14, by omega, by omega, by omega⟩

theorem sig71214_hyperbolic : sig71214.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig71214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71214_hard : cert_sig71214.isHard = true := by native_decide

-- (7,12,15): κ=0.2929 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (7,12,15): κ = 0.292857. -/
def sig71215 : BealFoundry.Signature := ⟨7, 12, 15, by omega, by omega, by omega⟩

theorem sig71215_hyperbolic : sig71215.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig71215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71215_hard : cert_sig71215.isHard = true := by native_decide

-- (7,13,13): κ=0.2967 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (7,13,13): κ = 0.296703. -/
def sig71313 : BealFoundry.Signature := ⟨7, 13, 13, by omega, by omega, by omega⟩

theorem sig71313_hyperbolic : sig71313.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig71313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71313_hard : cert_sig71313.isHard = true := by native_decide

-- (7,13,14): κ=0.2912 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (7,13,14): κ = 0.291209. -/
def sig71314 : BealFoundry.Signature := ⟨7, 13, 14, by omega, by omega, by omega⟩

theorem sig71314_hyperbolic : sig71314.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig71314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71314_hard : cert_sig71314.isHard = true := by native_decide

-- (7,13,15): κ=0.2864 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (7,13,15): κ = 0.286447. -/
def sig71315 : BealFoundry.Signature := ⟨7, 13, 15, by omega, by omega, by omega⟩

theorem sig71315_hyperbolic : sig71315.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig71315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71315_hard : cert_sig71315.isHard = true := by native_decide

-- (7,14,14): κ=0.2857 [hyperbolic] solutions=[] bound=1000 [50.0ms]
/-- (7,14,14): κ = 0.285714. -/
def sig71414 : BealFoundry.Signature := ⟨7, 14, 14, by omega, by omega, by omega⟩

theorem sig71414_hyperbolic : sig71414.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig71414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71414_hard : cert_sig71414.isHard = true := by native_decide

-- (7,14,15): κ=0.2810 [hyperbolic] solutions=[] bound=1000 [50.0ms]
/-- (7,14,15): κ = 0.280952. -/
def sig71415 : BealFoundry.Signature := ⟨7, 14, 15, by omega, by omega, by omega⟩

theorem sig71415_hyperbolic : sig71415.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig71415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71415_hard : cert_sig71415.isHard = true := by native_decide

-- (7,15,15): κ=0.2762 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (7,15,15): κ = 0.276190. -/
def sig71515 : BealFoundry.Signature := ⟨7, 15, 15, by omega, by omega, by omega⟩

theorem sig71515_hyperbolic : sig71515.regime = .hyperbolic := by
  native_decide

/-- Searched a^7 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig71515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig71515_hard : cert_sig71515.isHard = true := by native_decide

-- (8,8,8): κ=0.3750 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (8,8,8): κ = 0.375000. -/
def sig888 : BealFoundry.Signature := ⟨8, 8, 8, by omega, by omega, by omega⟩

theorem sig888_hyperbolic : sig888.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^8 up to 1000: no coprime solutions. -/
def cert_sig888 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig888_hard : cert_sig888.isHard = true := by native_decide

-- (8,8,9): κ=0.3611 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (8,8,9): κ = 0.361111. -/
def sig889 : BealFoundry.Signature := ⟨8, 8, 9, by omega, by omega, by omega⟩

theorem sig889_hyperbolic : sig889.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^9 up to 1000: no coprime solutions. -/
def cert_sig889 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig889_hard : cert_sig889.isHard = true := by native_decide

-- (8,8,10): κ=0.3500 [hyperbolic] solutions=[] bound=1000 [48.6ms]
/-- (8,8,10): κ = 0.350000. -/
def sig8810 : BealFoundry.Signature := ⟨8, 8, 10, by omega, by omega, by omega⟩

theorem sig8810_hyperbolic : sig8810.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^10 up to 1000: no coprime solutions. -/
def cert_sig8810 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8810_hard : cert_sig8810.isHard = true := by native_decide

-- (8,8,11): κ=0.3409 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (8,8,11): κ = 0.340909. -/
def sig8811 : BealFoundry.Signature := ⟨8, 8, 11, by omega, by omega, by omega⟩

theorem sig8811_hyperbolic : sig8811.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^11 up to 1000: no coprime solutions. -/
def cert_sig8811 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8811_hard : cert_sig8811.isHard = true := by native_decide

-- (8,8,12): κ=0.3333 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (8,8,12): κ = 0.333333. -/
def sig8812 : BealFoundry.Signature := ⟨8, 8, 12, by omega, by omega, by omega⟩

theorem sig8812_hyperbolic : sig8812.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^12 up to 1000: no coprime solutions. -/
def cert_sig8812 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8812_hard : cert_sig8812.isHard = true := by native_decide

-- (8,8,13): κ=0.3269 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (8,8,13): κ = 0.326923. -/
def sig8813 : BealFoundry.Signature := ⟨8, 8, 13, by omega, by omega, by omega⟩

theorem sig8813_hyperbolic : sig8813.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^13 up to 1000: no coprime solutions. -/
def cert_sig8813 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8813_hard : cert_sig8813.isHard = true := by native_decide

-- (8,8,14): κ=0.3214 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (8,8,14): κ = 0.321429. -/
def sig8814 : BealFoundry.Signature := ⟨8, 8, 14, by omega, by omega, by omega⟩

theorem sig8814_hyperbolic : sig8814.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^14 up to 1000: no coprime solutions. -/
def cert_sig8814 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8814_hard : cert_sig8814.isHard = true := by native_decide

-- (8,8,15): κ=0.3167 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (8,8,15): κ = 0.316667. -/
def sig8815 : BealFoundry.Signature := ⟨8, 8, 15, by omega, by omega, by omega⟩

theorem sig8815_hyperbolic : sig8815.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^8 = c^15 up to 1000: no coprime solutions. -/
def cert_sig8815 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8815_hard : cert_sig8815.isHard = true := by native_decide

-- (8,9,9): κ=0.3472 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (8,9,9): κ = 0.347222. -/
def sig899 : BealFoundry.Signature := ⟨8, 9, 9, by omega, by omega, by omega⟩

theorem sig899_hyperbolic : sig899.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^9 = c^9 up to 1000: no coprime solutions. -/
def cert_sig899 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig899_hard : cert_sig899.isHard = true := by native_decide

-- (8,9,10): κ=0.3361 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (8,9,10): κ = 0.336111. -/
def sig8910 : BealFoundry.Signature := ⟨8, 9, 10, by omega, by omega, by omega⟩

theorem sig8910_hyperbolic : sig8910.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^9 = c^10 up to 1000: no coprime solutions. -/
def cert_sig8910 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8910_hard : cert_sig8910.isHard = true := by native_decide

-- (8,9,11): κ=0.3270 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (8,9,11): κ = 0.327020. -/
def sig8911 : BealFoundry.Signature := ⟨8, 9, 11, by omega, by omega, by omega⟩

theorem sig8911_hyperbolic : sig8911.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^9 = c^11 up to 1000: no coprime solutions. -/
def cert_sig8911 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8911_hard : cert_sig8911.isHard = true := by native_decide

-- (8,9,12): κ=0.3194 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (8,9,12): κ = 0.319444. -/
def sig8912 : BealFoundry.Signature := ⟨8, 9, 12, by omega, by omega, by omega⟩

theorem sig8912_hyperbolic : sig8912.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^9 = c^12 up to 1000: no coprime solutions. -/
def cert_sig8912 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8912_hard : cert_sig8912.isHard = true := by native_decide

-- (8,9,13): κ=0.3130 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (8,9,13): κ = 0.313034. -/
def sig8913 : BealFoundry.Signature := ⟨8, 9, 13, by omega, by omega, by omega⟩

theorem sig8913_hyperbolic : sig8913.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^9 = c^13 up to 1000: no coprime solutions. -/
def cert_sig8913 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8913_hard : cert_sig8913.isHard = true := by native_decide

-- (8,9,14): κ=0.3075 [hyperbolic] solutions=[] bound=1000 [54.5ms]
/-- (8,9,14): κ = 0.307540. -/
def sig8914 : BealFoundry.Signature := ⟨8, 9, 14, by omega, by omega, by omega⟩

theorem sig8914_hyperbolic : sig8914.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^9 = c^14 up to 1000: no coprime solutions. -/
def cert_sig8914 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8914_hard : cert_sig8914.isHard = true := by native_decide

-- (8,9,15): κ=0.3028 [hyperbolic] solutions=[] bound=1000 [59.3ms]
/-- (8,9,15): κ = 0.302778. -/
def sig8915 : BealFoundry.Signature := ⟨8, 9, 15, by omega, by omega, by omega⟩

theorem sig8915_hyperbolic : sig8915.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^9 = c^15 up to 1000: no coprime solutions. -/
def cert_sig8915 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig8915_hard : cert_sig8915.isHard = true := by native_decide

-- (8,10,10): κ=0.3250 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (8,10,10): κ = 0.325000. -/
def sig81010 : BealFoundry.Signature := ⟨8, 10, 10, by omega, by omega, by omega⟩

theorem sig81010_hyperbolic : sig81010.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig81010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81010_hard : cert_sig81010.isHard = true := by native_decide

-- (8,10,11): κ=0.3159 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (8,10,11): κ = 0.315909. -/
def sig81011 : BealFoundry.Signature := ⟨8, 10, 11, by omega, by omega, by omega⟩

theorem sig81011_hyperbolic : sig81011.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig81011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81011_hard : cert_sig81011.isHard = true := by native_decide

-- (8,10,12): κ=0.3083 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (8,10,12): κ = 0.308333. -/
def sig81012 : BealFoundry.Signature := ⟨8, 10, 12, by omega, by omega, by omega⟩

theorem sig81012_hyperbolic : sig81012.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig81012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81012_hard : cert_sig81012.isHard = true := by native_decide

-- (8,10,13): κ=0.3019 [hyperbolic] solutions=[] bound=1000 [48.7ms]
/-- (8,10,13): κ = 0.301923. -/
def sig81013 : BealFoundry.Signature := ⟨8, 10, 13, by omega, by omega, by omega⟩

theorem sig81013_hyperbolic : sig81013.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig81013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81013_hard : cert_sig81013.isHard = true := by native_decide

-- (8,10,14): κ=0.2964 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (8,10,14): κ = 0.296429. -/
def sig81014 : BealFoundry.Signature := ⟨8, 10, 14, by omega, by omega, by omega⟩

theorem sig81014_hyperbolic : sig81014.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig81014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81014_hard : cert_sig81014.isHard = true := by native_decide

-- (8,10,15): κ=0.2917 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (8,10,15): κ = 0.291667. -/
def sig81015 : BealFoundry.Signature := ⟨8, 10, 15, by omega, by omega, by omega⟩

theorem sig81015_hyperbolic : sig81015.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig81015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81015_hard : cert_sig81015.isHard = true := by native_decide

-- (8,11,11): κ=0.3068 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (8,11,11): κ = 0.306818. -/
def sig81111 : BealFoundry.Signature := ⟨8, 11, 11, by omega, by omega, by omega⟩

theorem sig81111_hyperbolic : sig81111.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig81111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81111_hard : cert_sig81111.isHard = true := by native_decide

-- (8,11,12): κ=0.2992 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (8,11,12): κ = 0.299242. -/
def sig81112 : BealFoundry.Signature := ⟨8, 11, 12, by omega, by omega, by omega⟩

theorem sig81112_hyperbolic : sig81112.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig81112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81112_hard : cert_sig81112.isHard = true := by native_decide

-- (8,11,13): κ=0.2928 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (8,11,13): κ = 0.292832. -/
def sig81113 : BealFoundry.Signature := ⟨8, 11, 13, by omega, by omega, by omega⟩

theorem sig81113_hyperbolic : sig81113.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig81113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81113_hard : cert_sig81113.isHard = true := by native_decide

-- (8,11,14): κ=0.2873 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (8,11,14): κ = 0.287338. -/
def sig81114 : BealFoundry.Signature := ⟨8, 11, 14, by omega, by omega, by omega⟩

theorem sig81114_hyperbolic : sig81114.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig81114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81114_hard : cert_sig81114.isHard = true := by native_decide

-- (8,11,15): κ=0.2826 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (8,11,15): κ = 0.282576. -/
def sig81115 : BealFoundry.Signature := ⟨8, 11, 15, by omega, by omega, by omega⟩

theorem sig81115_hyperbolic : sig81115.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig81115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81115_hard : cert_sig81115.isHard = true := by native_decide

-- (8,12,12): κ=0.2917 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (8,12,12): κ = 0.291667. -/
def sig81212 : BealFoundry.Signature := ⟨8, 12, 12, by omega, by omega, by omega⟩

theorem sig81212_hyperbolic : sig81212.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig81212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81212_hard : cert_sig81212.isHard = true := by native_decide

-- (8,12,13): κ=0.2853 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (8,12,13): κ = 0.285256. -/
def sig81213 : BealFoundry.Signature := ⟨8, 12, 13, by omega, by omega, by omega⟩

theorem sig81213_hyperbolic : sig81213.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig81213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81213_hard : cert_sig81213.isHard = true := by native_decide

-- (8,12,14): κ=0.2798 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (8,12,14): κ = 0.279762. -/
def sig81214 : BealFoundry.Signature := ⟨8, 12, 14, by omega, by omega, by omega⟩

theorem sig81214_hyperbolic : sig81214.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig81214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81214_hard : cert_sig81214.isHard = true := by native_decide

-- (8,12,15): κ=0.2750 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (8,12,15): κ = 0.275000. -/
def sig81215 : BealFoundry.Signature := ⟨8, 12, 15, by omega, by omega, by omega⟩

theorem sig81215_hyperbolic : sig81215.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig81215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81215_hard : cert_sig81215.isHard = true := by native_decide

-- (8,13,13): κ=0.2788 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (8,13,13): κ = 0.278846. -/
def sig81313 : BealFoundry.Signature := ⟨8, 13, 13, by omega, by omega, by omega⟩

theorem sig81313_hyperbolic : sig81313.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig81313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81313_hard : cert_sig81313.isHard = true := by native_decide

-- (8,13,14): κ=0.2734 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (8,13,14): κ = 0.273352. -/
def sig81314 : BealFoundry.Signature := ⟨8, 13, 14, by omega, by omega, by omega⟩

theorem sig81314_hyperbolic : sig81314.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig81314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81314_hard : cert_sig81314.isHard = true := by native_decide

-- (8,13,15): κ=0.2686 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (8,13,15): κ = 0.268590. -/
def sig81315 : BealFoundry.Signature := ⟨8, 13, 15, by omega, by omega, by omega⟩

theorem sig81315_hyperbolic : sig81315.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig81315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81315_hard : cert_sig81315.isHard = true := by native_decide

-- (8,14,14): κ=0.2679 [hyperbolic] solutions=[] bound=1000 [50.1ms]
/-- (8,14,14): κ = 0.267857. -/
def sig81414 : BealFoundry.Signature := ⟨8, 14, 14, by omega, by omega, by omega⟩

theorem sig81414_hyperbolic : sig81414.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig81414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81414_hard : cert_sig81414.isHard = true := by native_decide

-- (8,14,15): κ=0.2631 [hyperbolic] solutions=[] bound=1000 [50.2ms]
/-- (8,14,15): κ = 0.263095. -/
def sig81415 : BealFoundry.Signature := ⟨8, 14, 15, by omega, by omega, by omega⟩

theorem sig81415_hyperbolic : sig81415.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig81415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81415_hard : cert_sig81415.isHard = true := by native_decide

-- (8,15,15): κ=0.2583 [hyperbolic] solutions=[] bound=1000 [50.4ms]
/-- (8,15,15): κ = 0.258333. -/
def sig81515 : BealFoundry.Signature := ⟨8, 15, 15, by omega, by omega, by omega⟩

theorem sig81515_hyperbolic : sig81515.regime = .hyperbolic := by
  native_decide

/-- Searched a^8 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig81515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig81515_hard : cert_sig81515.isHard = true := by native_decide

-- (9,9,9): κ=0.3333 [hyperbolic] solutions=[] bound=1000 [48.8ms]
/-- (9,9,9): κ = 0.333333. -/
def sig999 : BealFoundry.Signature := ⟨9, 9, 9, by omega, by omega, by omega⟩

theorem sig999_hyperbolic : sig999.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^9 = c^9 up to 1000: no coprime solutions. -/
def cert_sig999 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig999_hard : cert_sig999.isHard = true := by native_decide

-- (9,9,10): κ=0.3222 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (9,9,10): κ = 0.322222. -/
def sig9910 : BealFoundry.Signature := ⟨9, 9, 10, by omega, by omega, by omega⟩

theorem sig9910_hyperbolic : sig9910.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^9 = c^10 up to 1000: no coprime solutions. -/
def cert_sig9910 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig9910_hard : cert_sig9910.isHard = true := by native_decide

-- (9,9,11): κ=0.3131 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (9,9,11): κ = 0.313131. -/
def sig9911 : BealFoundry.Signature := ⟨9, 9, 11, by omega, by omega, by omega⟩

theorem sig9911_hyperbolic : sig9911.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^9 = c^11 up to 1000: no coprime solutions. -/
def cert_sig9911 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig9911_hard : cert_sig9911.isHard = true := by native_decide

-- (9,9,12): κ=0.3056 [hyperbolic] solutions=[] bound=1000 [48.5ms]
/-- (9,9,12): κ = 0.305556. -/
def sig9912 : BealFoundry.Signature := ⟨9, 9, 12, by omega, by omega, by omega⟩

theorem sig9912_hyperbolic : sig9912.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^9 = c^12 up to 1000: no coprime solutions. -/
def cert_sig9912 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig9912_hard : cert_sig9912.isHard = true := by native_decide

-- (9,9,13): κ=0.2991 [hyperbolic] solutions=[] bound=1000 [48.3ms]
/-- (9,9,13): κ = 0.299145. -/
def sig9913 : BealFoundry.Signature := ⟨9, 9, 13, by omega, by omega, by omega⟩

theorem sig9913_hyperbolic : sig9913.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^9 = c^13 up to 1000: no coprime solutions. -/
def cert_sig9913 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig9913_hard : cert_sig9913.isHard = true := by native_decide

-- (9,9,14): κ=0.2937 [hyperbolic] solutions=[] bound=1000 [48.2ms]
/-- (9,9,14): κ = 0.293651. -/
def sig9914 : BealFoundry.Signature := ⟨9, 9, 14, by omega, by omega, by omega⟩

theorem sig9914_hyperbolic : sig9914.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^9 = c^14 up to 1000: no coprime solutions. -/
def cert_sig9914 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig9914_hard : cert_sig9914.isHard = true := by native_decide

-- (9,9,15): κ=0.2889 [hyperbolic] solutions=[] bound=1000 [48.4ms]
/-- (9,9,15): κ = 0.288889. -/
def sig9915 : BealFoundry.Signature := ⟨9, 9, 15, by omega, by omega, by omega⟩

theorem sig9915_hyperbolic : sig9915.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^9 = c^15 up to 1000: no coprime solutions. -/
def cert_sig9915 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig9915_hard : cert_sig9915.isHard = true := by native_decide

-- (9,10,10): κ=0.3111 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,10,10): κ = 0.311111. -/
def sig91010 : BealFoundry.Signature := ⟨9, 10, 10, by omega, by omega, by omega⟩

theorem sig91010_hyperbolic : sig91010.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig91010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91010_hard : cert_sig91010.isHard = true := by native_decide

-- (9,10,11): κ=0.3020 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (9,10,11): κ = 0.302020. -/
def sig91011 : BealFoundry.Signature := ⟨9, 10, 11, by omega, by omega, by omega⟩

theorem sig91011_hyperbolic : sig91011.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig91011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91011_hard : cert_sig91011.isHard = true := by native_decide

-- (9,10,12): κ=0.2944 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (9,10,12): κ = 0.294444. -/
def sig91012 : BealFoundry.Signature := ⟨9, 10, 12, by omega, by omega, by omega⟩

theorem sig91012_hyperbolic : sig91012.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig91012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91012_hard : cert_sig91012.isHard = true := by native_decide

-- (9,10,13): κ=0.2880 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,10,13): κ = 0.288034. -/
def sig91013 : BealFoundry.Signature := ⟨9, 10, 13, by omega, by omega, by omega⟩

theorem sig91013_hyperbolic : sig91013.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig91013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91013_hard : cert_sig91013.isHard = true := by native_decide

-- (9,10,14): κ=0.2825 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (9,10,14): κ = 0.282540. -/
def sig91014 : BealFoundry.Signature := ⟨9, 10, 14, by omega, by omega, by omega⟩

theorem sig91014_hyperbolic : sig91014.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig91014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91014_hard : cert_sig91014.isHard = true := by native_decide

-- (9,10,15): κ=0.2778 [hyperbolic] solutions=[] bound=1000 [49.0ms]
/-- (9,10,15): κ = 0.277778. -/
def sig91015 : BealFoundry.Signature := ⟨9, 10, 15, by omega, by omega, by omega⟩

theorem sig91015_hyperbolic : sig91015.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig91015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91015_hard : cert_sig91015.isHard = true := by native_decide

-- (9,11,11): κ=0.2929 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (9,11,11): κ = 0.292929. -/
def sig91111 : BealFoundry.Signature := ⟨9, 11, 11, by omega, by omega, by omega⟩

theorem sig91111_hyperbolic : sig91111.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig91111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91111_hard : cert_sig91111.isHard = true := by native_decide

-- (9,11,12): κ=0.2854 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (9,11,12): κ = 0.285354. -/
def sig91112 : BealFoundry.Signature := ⟨9, 11, 12, by omega, by omega, by omega⟩

theorem sig91112_hyperbolic : sig91112.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig91112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91112_hard : cert_sig91112.isHard = true := by native_decide

-- (9,11,13): κ=0.2789 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,11,13): κ = 0.278943. -/
def sig91113 : BealFoundry.Signature := ⟨9, 11, 13, by omega, by omega, by omega⟩

theorem sig91113_hyperbolic : sig91113.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig91113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91113_hard : cert_sig91113.isHard = true := by native_decide

-- (9,11,14): κ=0.2734 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (9,11,14): κ = 0.273449. -/
def sig91114 : BealFoundry.Signature := ⟨9, 11, 14, by omega, by omega, by omega⟩

theorem sig91114_hyperbolic : sig91114.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig91114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91114_hard : cert_sig91114.isHard = true := by native_decide

-- (9,11,15): κ=0.2687 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,11,15): κ = 0.268687. -/
def sig91115 : BealFoundry.Signature := ⟨9, 11, 15, by omega, by omega, by omega⟩

theorem sig91115_hyperbolic : sig91115.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig91115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91115_hard : cert_sig91115.isHard = true := by native_decide

-- (9,12,12): κ=0.2778 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (9,12,12): κ = 0.277778. -/
def sig91212 : BealFoundry.Signature := ⟨9, 12, 12, by omega, by omega, by omega⟩

theorem sig91212_hyperbolic : sig91212.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig91212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91212_hard : cert_sig91212.isHard = true := by native_decide

-- (9,12,13): κ=0.2714 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (9,12,13): κ = 0.271368. -/
def sig91213 : BealFoundry.Signature := ⟨9, 12, 13, by omega, by omega, by omega⟩

theorem sig91213_hyperbolic : sig91213.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig91213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91213_hard : cert_sig91213.isHard = true := by native_decide

-- (9,12,14): κ=0.2659 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (9,12,14): κ = 0.265873. -/
def sig91214 : BealFoundry.Signature := ⟨9, 12, 14, by omega, by omega, by omega⟩

theorem sig91214_hyperbolic : sig91214.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig91214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91214_hard : cert_sig91214.isHard = true := by native_decide

-- (9,12,15): κ=0.2611 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,12,15): κ = 0.261111. -/
def sig91215 : BealFoundry.Signature := ⟨9, 12, 15, by omega, by omega, by omega⟩

theorem sig91215_hyperbolic : sig91215.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig91215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91215_hard : cert_sig91215.isHard = true := by native_decide

-- (9,13,13): κ=0.2650 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,13,13): κ = 0.264957. -/
def sig91313 : BealFoundry.Signature := ⟨9, 13, 13, by omega, by omega, by omega⟩

theorem sig91313_hyperbolic : sig91313.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig91313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91313_hard : cert_sig91313.isHard = true := by native_decide

-- (9,13,14): κ=0.2595 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (9,13,14): κ = 0.259463. -/
def sig91314 : BealFoundry.Signature := ⟨9, 13, 14, by omega, by omega, by omega⟩

theorem sig91314_hyperbolic : sig91314.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig91314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91314_hard : cert_sig91314.isHard = true := by native_decide

-- (9,13,15): κ=0.2547 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,13,15): κ = 0.254701. -/
def sig91315 : BealFoundry.Signature := ⟨9, 13, 15, by omega, by omega, by omega⟩

theorem sig91315_hyperbolic : sig91315.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig91315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91315_hard : cert_sig91315.isHard = true := by native_decide

-- (9,14,14): κ=0.2540 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (9,14,14): κ = 0.253968. -/
def sig91414 : BealFoundry.Signature := ⟨9, 14, 14, by omega, by omega, by omega⟩

theorem sig91414_hyperbolic : sig91414.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig91414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91414_hard : cert_sig91414.isHard = true := by native_decide

-- (9,14,15): κ=0.2492 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (9,14,15): κ = 0.249206. -/
def sig91415 : BealFoundry.Signature := ⟨9, 14, 15, by omega, by omega, by omega⟩

theorem sig91415_hyperbolic : sig91415.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig91415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91415_hard : cert_sig91415.isHard = true := by native_decide

-- (9,15,15): κ=0.2444 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (9,15,15): κ = 0.244444. -/
def sig91515 : BealFoundry.Signature := ⟨9, 15, 15, by omega, by omega, by omega⟩

theorem sig91515_hyperbolic : sig91515.regime = .hyperbolic := by
  native_decide

/-- Searched a^9 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig91515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig91515_hard : cert_sig91515.isHard = true := by native_decide

-- (10,10,10): κ=0.3000 [hyperbolic] solutions=[] bound=1000 [48.9ms]
/-- (10,10,10): κ = 0.300000. -/
def sig101010 : BealFoundry.Signature := ⟨10, 10, 10, by omega, by omega, by omega⟩

theorem sig101010_hyperbolic : sig101010.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^10 = c^10 up to 1000: no coprime solutions. -/
def cert_sig101010 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101010_hard : cert_sig101010.isHard = true := by native_decide

-- (10,10,11): κ=0.2909 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (10,10,11): κ = 0.290909. -/
def sig101011 : BealFoundry.Signature := ⟨10, 10, 11, by omega, by omega, by omega⟩

theorem sig101011_hyperbolic : sig101011.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^10 = c^11 up to 1000: no coprime solutions. -/
def cert_sig101011 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101011_hard : cert_sig101011.isHard = true := by native_decide

-- (10,10,12): κ=0.2833 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (10,10,12): κ = 0.283333. -/
def sig101012 : BealFoundry.Signature := ⟨10, 10, 12, by omega, by omega, by omega⟩

theorem sig101012_hyperbolic : sig101012.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^10 = c^12 up to 1000: no coprime solutions. -/
def cert_sig101012 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101012_hard : cert_sig101012.isHard = true := by native_decide

-- (10,10,13): κ=0.2769 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (10,10,13): κ = 0.276923. -/
def sig101013 : BealFoundry.Signature := ⟨10, 10, 13, by omega, by omega, by omega⟩

theorem sig101013_hyperbolic : sig101013.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^10 = c^13 up to 1000: no coprime solutions. -/
def cert_sig101013 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101013_hard : cert_sig101013.isHard = true := by native_decide

-- (10,10,14): κ=0.2714 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (10,10,14): κ = 0.271429. -/
def sig101014 : BealFoundry.Signature := ⟨10, 10, 14, by omega, by omega, by omega⟩

theorem sig101014_hyperbolic : sig101014.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^10 = c^14 up to 1000: no coprime solutions. -/
def cert_sig101014 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101014_hard : cert_sig101014.isHard = true := by native_decide

-- (10,10,15): κ=0.2667 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (10,10,15): κ = 0.266667. -/
def sig101015 : BealFoundry.Signature := ⟨10, 10, 15, by omega, by omega, by omega⟩

theorem sig101015_hyperbolic : sig101015.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^10 = c^15 up to 1000: no coprime solutions. -/
def cert_sig101015 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101015_hard : cert_sig101015.isHard = true := by native_decide

-- (10,11,11): κ=0.2818 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (10,11,11): κ = 0.281818. -/
def sig101111 : BealFoundry.Signature := ⟨10, 11, 11, by omega, by omega, by omega⟩

theorem sig101111_hyperbolic : sig101111.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig101111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101111_hard : cert_sig101111.isHard = true := by native_decide

-- (10,11,12): κ=0.2742 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (10,11,12): κ = 0.274242. -/
def sig101112 : BealFoundry.Signature := ⟨10, 11, 12, by omega, by omega, by omega⟩

theorem sig101112_hyperbolic : sig101112.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig101112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101112_hard : cert_sig101112.isHard = true := by native_decide

-- (10,11,13): κ=0.2678 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (10,11,13): κ = 0.267832. -/
def sig101113 : BealFoundry.Signature := ⟨10, 11, 13, by omega, by omega, by omega⟩

theorem sig101113_hyperbolic : sig101113.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig101113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101113_hard : cert_sig101113.isHard = true := by native_decide

-- (10,11,14): κ=0.2623 [hyperbolic] solutions=[] bound=1000 [51.5ms]
/-- (10,11,14): κ = 0.262338. -/
def sig101114 : BealFoundry.Signature := ⟨10, 11, 14, by omega, by omega, by omega⟩

theorem sig101114_hyperbolic : sig101114.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig101114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101114_hard : cert_sig101114.isHard = true := by native_decide

-- (10,11,15): κ=0.2576 [hyperbolic] solutions=[] bound=1000 [52.1ms]
/-- (10,11,15): κ = 0.257576. -/
def sig101115 : BealFoundry.Signature := ⟨10, 11, 15, by omega, by omega, by omega⟩

theorem sig101115_hyperbolic : sig101115.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig101115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101115_hard : cert_sig101115.isHard = true := by native_decide

-- (10,12,12): κ=0.2667 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (10,12,12): κ = 0.266667. -/
def sig101212 : BealFoundry.Signature := ⟨10, 12, 12, by omega, by omega, by omega⟩

theorem sig101212_hyperbolic : sig101212.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig101212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101212_hard : cert_sig101212.isHard = true := by native_decide

-- (10,12,13): κ=0.2603 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (10,12,13): κ = 0.260256. -/
def sig101213 : BealFoundry.Signature := ⟨10, 12, 13, by omega, by omega, by omega⟩

theorem sig101213_hyperbolic : sig101213.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig101213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101213_hard : cert_sig101213.isHard = true := by native_decide

-- (10,12,14): κ=0.2548 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (10,12,14): κ = 0.254762. -/
def sig101214 : BealFoundry.Signature := ⟨10, 12, 14, by omega, by omega, by omega⟩

theorem sig101214_hyperbolic : sig101214.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig101214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101214_hard : cert_sig101214.isHard = true := by native_decide

-- (10,12,15): κ=0.2500 [hyperbolic] solutions=[] bound=1000 [49.2ms]
/-- (10,12,15): κ = 0.250000. -/
def sig101215 : BealFoundry.Signature := ⟨10, 12, 15, by omega, by omega, by omega⟩

theorem sig101215_hyperbolic : sig101215.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig101215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101215_hard : cert_sig101215.isHard = true := by native_decide

-- (10,13,13): κ=0.2538 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (10,13,13): κ = 0.253846. -/
def sig101313 : BealFoundry.Signature := ⟨10, 13, 13, by omega, by omega, by omega⟩

theorem sig101313_hyperbolic : sig101313.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig101313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101313_hard : cert_sig101313.isHard = true := by native_decide

-- (10,13,14): κ=0.2484 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (10,13,14): κ = 0.248352. -/
def sig101314 : BealFoundry.Signature := ⟨10, 13, 14, by omega, by omega, by omega⟩

theorem sig101314_hyperbolic : sig101314.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig101314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101314_hard : cert_sig101314.isHard = true := by native_decide

-- (10,13,15): κ=0.2436 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (10,13,15): κ = 0.243590. -/
def sig101315 : BealFoundry.Signature := ⟨10, 13, 15, by omega, by omega, by omega⟩

theorem sig101315_hyperbolic : sig101315.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig101315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101315_hard : cert_sig101315.isHard = true := by native_decide

-- (10,14,14): κ=0.2429 [hyperbolic] solutions=[] bound=1000 [50.0ms]
/-- (10,14,14): κ = 0.242857. -/
def sig101414 : BealFoundry.Signature := ⟨10, 14, 14, by omega, by omega, by omega⟩

theorem sig101414_hyperbolic : sig101414.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig101414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101414_hard : cert_sig101414.isHard = true := by native_decide

-- (10,14,15): κ=0.2381 [hyperbolic] solutions=[] bound=1000 [50.2ms]
/-- (10,14,15): κ = 0.238095. -/
def sig101415 : BealFoundry.Signature := ⟨10, 14, 15, by omega, by omega, by omega⟩

theorem sig101415_hyperbolic : sig101415.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig101415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101415_hard : cert_sig101415.isHard = true := by native_decide

-- (10,15,15): κ=0.2333 [hyperbolic] solutions=[] bound=1000 [50.3ms]
/-- (10,15,15): κ = 0.233333. -/
def sig101515 : BealFoundry.Signature := ⟨10, 15, 15, by omega, by omega, by omega⟩

theorem sig101515_hyperbolic : sig101515.regime = .hyperbolic := by
  native_decide

/-- Searched a^10 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig101515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig101515_hard : cert_sig101515.isHard = true := by native_decide

-- (11,11,11): κ=0.2727 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (11,11,11): κ = 0.272727. -/
def sig111111 : BealFoundry.Signature := ⟨11, 11, 11, by omega, by omega, by omega⟩

theorem sig111111_hyperbolic : sig111111.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^11 = c^11 up to 1000: no coprime solutions. -/
def cert_sig111111 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111111_hard : cert_sig111111.isHard = true := by native_decide

-- (11,11,12): κ=0.2652 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (11,11,12): κ = 0.265152. -/
def sig111112 : BealFoundry.Signature := ⟨11, 11, 12, by omega, by omega, by omega⟩

theorem sig111112_hyperbolic : sig111112.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^11 = c^12 up to 1000: no coprime solutions. -/
def cert_sig111112 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111112_hard : cert_sig111112.isHard = true := by native_decide

-- (11,11,13): κ=0.2587 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (11,11,13): κ = 0.258741. -/
def sig111113 : BealFoundry.Signature := ⟨11, 11, 13, by omega, by omega, by omega⟩

theorem sig111113_hyperbolic : sig111113.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^11 = c^13 up to 1000: no coprime solutions. -/
def cert_sig111113 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111113_hard : cert_sig111113.isHard = true := by native_decide

-- (11,11,14): κ=0.2532 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (11,11,14): κ = 0.253247. -/
def sig111114 : BealFoundry.Signature := ⟨11, 11, 14, by omega, by omega, by omega⟩

theorem sig111114_hyperbolic : sig111114.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^11 = c^14 up to 1000: no coprime solutions. -/
def cert_sig111114 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111114_hard : cert_sig111114.isHard = true := by native_decide

-- (11,11,15): κ=0.2485 [hyperbolic] solutions=[] bound=1000 [51.6ms]
/-- (11,11,15): κ = 0.248485. -/
def sig111115 : BealFoundry.Signature := ⟨11, 11, 15, by omega, by omega, by omega⟩

theorem sig111115_hyperbolic : sig111115.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^11 = c^15 up to 1000: no coprime solutions. -/
def cert_sig111115 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111115_hard : cert_sig111115.isHard = true := by native_decide

-- (11,12,12): κ=0.2576 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (11,12,12): κ = 0.257576. -/
def sig111212 : BealFoundry.Signature := ⟨11, 12, 12, by omega, by omega, by omega⟩

theorem sig111212_hyperbolic : sig111212.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig111212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111212_hard : cert_sig111212.isHard = true := by native_decide

-- (11,12,13): κ=0.2512 [hyperbolic] solutions=[] bound=1000 [49.1ms]
/-- (11,12,13): κ = 0.251166. -/
def sig111213 : BealFoundry.Signature := ⟨11, 12, 13, by omega, by omega, by omega⟩

theorem sig111213_hyperbolic : sig111213.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig111213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111213_hard : cert_sig111213.isHard = true := by native_decide

-- (11,12,14): κ=0.2457 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (11,12,14): κ = 0.245671. -/
def sig111214 : BealFoundry.Signature := ⟨11, 12, 14, by omega, by omega, by omega⟩

theorem sig111214_hyperbolic : sig111214.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig111214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111214_hard : cert_sig111214.isHard = true := by native_decide

-- (11,12,15): κ=0.2409 [hyperbolic] solutions=[] bound=1000 [49.6ms]
/-- (11,12,15): κ = 0.240909. -/
def sig111215 : BealFoundry.Signature := ⟨11, 12, 15, by omega, by omega, by omega⟩

theorem sig111215_hyperbolic : sig111215.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig111215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111215_hard : cert_sig111215.isHard = true := by native_decide

-- (11,13,13): κ=0.2448 [hyperbolic] solutions=[] bound=1000 [50.4ms]
/-- (11,13,13): κ = 0.244755. -/
def sig111313 : BealFoundry.Signature := ⟨11, 13, 13, by omega, by omega, by omega⟩

theorem sig111313_hyperbolic : sig111313.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig111313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111313_hard : cert_sig111313.isHard = true := by native_decide

-- (11,13,14): κ=0.2393 [hyperbolic] solutions=[] bound=1000 [49.7ms]
/-- (11,13,14): κ = 0.239261. -/
def sig111314 : BealFoundry.Signature := ⟨11, 13, 14, by omega, by omega, by omega⟩

theorem sig111314_hyperbolic : sig111314.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig111314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111314_hard : cert_sig111314.isHard = true := by native_decide

-- (11,13,15): κ=0.2345 [hyperbolic] solutions=[] bound=1000 [50.5ms]
/-- (11,13,15): κ = 0.234499. -/
def sig111315 : BealFoundry.Signature := ⟨11, 13, 15, by omega, by omega, by omega⟩

theorem sig111315_hyperbolic : sig111315.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig111315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111315_hard : cert_sig111315.isHard = true := by native_decide

-- (11,14,14): κ=0.2338 [hyperbolic] solutions=[] bound=1000 [50.2ms]
/-- (11,14,14): κ = 0.233766. -/
def sig111414 : BealFoundry.Signature := ⟨11, 14, 14, by omega, by omega, by omega⟩

theorem sig111414_hyperbolic : sig111414.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig111414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111414_hard : cert_sig111414.isHard = true := by native_decide

-- (11,14,15): κ=0.2290 [hyperbolic] solutions=[] bound=1000 [50.0ms]
/-- (11,14,15): κ = 0.229004. -/
def sig111415 : BealFoundry.Signature := ⟨11, 14, 15, by omega, by omega, by omega⟩

theorem sig111415_hyperbolic : sig111415.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig111415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111415_hard : cert_sig111415.isHard = true := by native_decide

-- (11,15,15): κ=0.2242 [hyperbolic] solutions=[] bound=1000 [50.4ms]
/-- (11,15,15): κ = 0.224242. -/
def sig111515 : BealFoundry.Signature := ⟨11, 15, 15, by omega, by omega, by omega⟩

theorem sig111515_hyperbolic : sig111515.regime = .hyperbolic := by
  native_decide

/-- Searched a^11 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig111515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig111515_hard : cert_sig111515.isHard = true := by native_decide

-- (12,12,12): κ=0.2500 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (12,12,12): κ = 0.250000. -/
def sig121212 : BealFoundry.Signature := ⟨12, 12, 12, by omega, by omega, by omega⟩

theorem sig121212_hyperbolic : sig121212.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^12 = c^12 up to 1000: no coprime solutions. -/
def cert_sig121212 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121212_hard : cert_sig121212.isHard = true := by native_decide

-- (12,12,13): κ=0.2436 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (12,12,13): κ = 0.243590. -/
def sig121213 : BealFoundry.Signature := ⟨12, 12, 13, by omega, by omega, by omega⟩

theorem sig121213_hyperbolic : sig121213.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^12 = c^13 up to 1000: no coprime solutions. -/
def cert_sig121213 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121213_hard : cert_sig121213.isHard = true := by native_decide

-- (12,12,14): κ=0.2381 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (12,12,14): κ = 0.238095. -/
def sig121214 : BealFoundry.Signature := ⟨12, 12, 14, by omega, by omega, by omega⟩

theorem sig121214_hyperbolic : sig121214.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^12 = c^14 up to 1000: no coprime solutions. -/
def cert_sig121214 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121214_hard : cert_sig121214.isHard = true := by native_decide

-- (12,12,15): κ=0.2333 [hyperbolic] solutions=[] bound=1000 [49.5ms]
/-- (12,12,15): κ = 0.233333. -/
def sig121215 : BealFoundry.Signature := ⟨12, 12, 15, by omega, by omega, by omega⟩

theorem sig121215_hyperbolic : sig121215.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^12 = c^15 up to 1000: no coprime solutions. -/
def cert_sig121215 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121215_hard : cert_sig121215.isHard = true := by native_decide

-- (12,13,13): κ=0.2372 [hyperbolic] solutions=[] bound=1000 [49.3ms]
/-- (12,13,13): κ = 0.237179. -/
def sig121313 : BealFoundry.Signature := ⟨12, 13, 13, by omega, by omega, by omega⟩

theorem sig121313_hyperbolic : sig121313.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig121313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121313_hard : cert_sig121313.isHard = true := by native_decide

-- (12,13,14): κ=0.2317 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (12,13,14): κ = 0.231685. -/
def sig121314 : BealFoundry.Signature := ⟨12, 13, 14, by omega, by omega, by omega⟩

theorem sig121314_hyperbolic : sig121314.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig121314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121314_hard : cert_sig121314.isHard = true := by native_decide

-- (12,13,15): κ=0.2269 [hyperbolic] solutions=[] bound=1000 [49.4ms]
/-- (12,13,15): κ = 0.226923. -/
def sig121315 : BealFoundry.Signature := ⟨12, 13, 15, by omega, by omega, by omega⟩

theorem sig121315_hyperbolic : sig121315.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig121315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121315_hard : cert_sig121315.isHard = true := by native_decide

-- (12,14,14): κ=0.2262 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (12,14,14): κ = 0.226190. -/
def sig121414 : BealFoundry.Signature := ⟨12, 14, 14, by omega, by omega, by omega⟩

theorem sig121414_hyperbolic : sig121414.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig121414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121414_hard : cert_sig121414.isHard = true := by native_decide

-- (12,14,15): κ=0.2214 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (12,14,15): κ = 0.221429. -/
def sig121415 : BealFoundry.Signature := ⟨12, 14, 15, by omega, by omega, by omega⟩

theorem sig121415_hyperbolic : sig121415.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig121415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121415_hard : cert_sig121415.isHard = true := by native_decide

-- (12,15,15): κ=0.2167 [hyperbolic] solutions=[] bound=1000 [49.8ms]
/-- (12,15,15): κ = 0.216667. -/
def sig121515 : BealFoundry.Signature := ⟨12, 15, 15, by omega, by omega, by omega⟩

theorem sig121515_hyperbolic : sig121515.regime = .hyperbolic := by
  native_decide

/-- Searched a^12 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig121515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig121515_hard : cert_sig121515.isHard = true := by native_decide

-- (13,13,13): κ=0.2308 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (13,13,13): κ = 0.230769. -/
def sig131313 : BealFoundry.Signature := ⟨13, 13, 13, by omega, by omega, by omega⟩

theorem sig131313_hyperbolic : sig131313.regime = .hyperbolic := by
  native_decide

/-- Searched a^13 + b^13 = c^13 up to 1000: no coprime solutions. -/
def cert_sig131313 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig131313_hard : cert_sig131313.isHard = true := by native_decide

-- (13,13,14): κ=0.2253 [hyperbolic] solutions=[] bound=1000 [49.9ms]
/-- (13,13,14): κ = 0.225275. -/
def sig131314 : BealFoundry.Signature := ⟨13, 13, 14, by omega, by omega, by omega⟩

theorem sig131314_hyperbolic : sig131314.regime = .hyperbolic := by
  native_decide

/-- Searched a^13 + b^13 = c^14 up to 1000: no coprime solutions. -/
def cert_sig131314 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig131314_hard : cert_sig131314.isHard = true := by native_decide

-- (13,13,15): κ=0.2205 [hyperbolic] solutions=[] bound=1000 [50.2ms]
/-- (13,13,15): κ = 0.220513. -/
def sig131315 : BealFoundry.Signature := ⟨13, 13, 15, by omega, by omega, by omega⟩

theorem sig131315_hyperbolic : sig131315.regime = .hyperbolic := by
  native_decide

/-- Searched a^13 + b^13 = c^15 up to 1000: no coprime solutions. -/
def cert_sig131315 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig131315_hard : cert_sig131315.isHard = true := by native_decide

-- (13,14,14): κ=0.2198 [hyperbolic] solutions=[] bound=1000 [50.3ms]
/-- (13,14,14): κ = 0.219780. -/
def sig131414 : BealFoundry.Signature := ⟨13, 14, 14, by omega, by omega, by omega⟩

theorem sig131414_hyperbolic : sig131414.regime = .hyperbolic := by
  native_decide

/-- Searched a^13 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig131414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig131414_hard : cert_sig131414.isHard = true := by native_decide

-- (13,14,15): κ=0.2150 [hyperbolic] solutions=[] bound=1000 [50.4ms]
/-- (13,14,15): κ = 0.215018. -/
def sig131415 : BealFoundry.Signature := ⟨13, 14, 15, by omega, by omega, by omega⟩

theorem sig131415_hyperbolic : sig131415.regime = .hyperbolic := by
  native_decide

/-- Searched a^13 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig131415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig131415_hard : cert_sig131415.isHard = true := by native_decide

-- (13,15,15): κ=0.2103 [hyperbolic] solutions=[] bound=1000 [50.3ms]
/-- (13,15,15): κ = 0.210256. -/
def sig131515 : BealFoundry.Signature := ⟨13, 15, 15, by omega, by omega, by omega⟩

theorem sig131515_hyperbolic : sig131515.regime = .hyperbolic := by
  native_decide

/-- Searched a^13 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig131515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig131515_hard : cert_sig131515.isHard = true := by native_decide

-- (14,14,14): κ=0.2143 [hyperbolic] solutions=[] bound=1000 [50.5ms]
/-- (14,14,14): κ = 0.214286. -/
def sig141414 : BealFoundry.Signature := ⟨14, 14, 14, by omega, by omega, by omega⟩

theorem sig141414_hyperbolic : sig141414.regime = .hyperbolic := by
  native_decide

/-- Searched a^14 + b^14 = c^14 up to 1000: no coprime solutions. -/
def cert_sig141414 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig141414_hard : cert_sig141414.isHard = true := by native_decide

-- (14,14,15): κ=0.2095 [hyperbolic] solutions=[] bound=1000 [50.2ms]
/-- (14,14,15): κ = 0.209524. -/
def sig141415 : BealFoundry.Signature := ⟨14, 14, 15, by omega, by omega, by omega⟩

theorem sig141415_hyperbolic : sig141415.regime = .hyperbolic := by
  native_decide

/-- Searched a^14 + b^14 = c^15 up to 1000: no coprime solutions. -/
def cert_sig141415 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig141415_hard : cert_sig141415.isHard = true := by native_decide

-- (14,15,15): κ=0.2048 [hyperbolic] solutions=[] bound=1000 [50.5ms]
/-- (14,15,15): κ = 0.204762. -/
def sig141515 : BealFoundry.Signature := ⟨14, 15, 15, by omega, by omega, by omega⟩

theorem sig141515_hyperbolic : sig141515.regime = .hyperbolic := by
  native_decide

/-- Searched a^14 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig141515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig141515_hard : cert_sig141515.isHard = true := by native_decide

-- (15,15,15): κ=0.2000 [hyperbolic] solutions=[] bound=1000 [50.9ms]
/-- (15,15,15): κ = 0.200000. -/
def sig151515 : BealFoundry.Signature := ⟨15, 15, 15, by omega, by omega, by omega⟩

theorem sig151515_hyperbolic : sig151515.regime = .hyperbolic := by
  native_decide

/-- Searched a^15 + b^15 = c^15 up to 1000: no coprime solutions. -/
def cert_sig151515 : BealFoundry.Certificate where
  certType := .computation
  reference := "Exhaustive search up to 1000"
  seam := "no coprime solutions found"
  verifier := "unified_solver"

theorem cert_sig151515_hard : cert_sig151515.isHard = true := by native_decide

-- Summary: 455/455 Beal signatures clear (no coprime solutions up to 1000)

end BealFoundry.Solver

namespace BealFoundry.NS

-- ═══════════════════════════════════════════════════════════════════
-- Navier-Stokes Adapter (stub)
-- ═══════════════════════════════════════════════════════════════════

/-- NS domain: scaling criticality. -/
def nsDomain : BealFoundry.Domain where
  name := "Navier-Stokes Regularity"
  lambdaKind := .scalingCriticality
  stateSpace := "Velocity fields u ∈ H¹(ℝ³) with ∇·u = 0"
  invariants := [
    "Energy inequality: ‖u(t)‖₂² + 2ν∫₀ᵗ‖∇u‖₂² ≤ ‖u₀‖₂²",
    "BKM criterion: ∫₀ᵗ ‖ω‖_∞ ds < ∞ ⟹ regularity"
  ]
  stopRule := "Global regularity proved OR finite-time blowup exhibited"
  falsifier := "Exhibit smooth initial data with finite-time singularity"
  state := .active

/-- Flux limiter connection: CONDITIONAL certificate.
    Depends on bridging CCO flux limiter to BKM criterion. -/
def nsFluxLimiterCert : BealFoundry.Certificate where
  certType := .conditional
  reference := "CCO flux limiter theorem → BKM criterion bridge"
  seam := "Flux limiter → enstrophy flux bound unproved"
  verifier := "pending"
  blockingSeam := some "CCO-to-PDE bridge formalization"

/-- The NS flux limiter certificate is NOT hard (conditional). -/
theorem nsFluxLimiterCert_not_hard :
    nsFluxLimiterCert.isHard = false := by native_decide

/-- Therefore it cannot support a proven lock (governance enforces). -/
theorem nsFluxLimiterCert_no_proven :
    ¬ BealFoundry.permittedLock nsFluxLimiterCert.certType .proven := by
  simp [nsFluxLimiterCert, BealFoundry.permittedLock, BealFoundry.CertificateType.isHard]

end BealFoundry.NS

namespace BealFoundry.Riemann

-- ═══════════════════════════════════════════════════════════════════
-- Riemann Hypothesis Adapter (stub)
-- ═══════════════════════════════════════════════════════════════════

/-- Riemann domain: spectral gap risk. -/
def riemannDomain : BealFoundry.Domain where
  name := "Riemann Hypothesis"
  lambdaKind := .spectralGapRisk
  stateSpace := "Non-trivial zeros of ζ(s) in critical strip 0 < Re(s) < 1"
  invariants := [
    "Functional equation: ζ(s) = 2^s π^(s-1) sin(πs/2) Γ(1-s) ζ(1-s)",
    "Euler product: ζ(s) = ∏_p (1 - p^(-s))^(-1) for Re(s) > 1"
  ]
  stopRule := "All zeros on Re(s) = 1/2 OR off-line zero exhibited"
  falsifier := "Exhibit ρ with ζ(ρ) = 0 and Re(ρ) ≠ 1/2"
  state := .active

/-- Spectral gap certificate: HEURISTIC only.
    Numerical evidence from 10^13 verified zeros. -/
def riemannSpectralCert : BealFoundry.Certificate where
  certType := .heuristic
  reference := "Platt 2021: first 10^13 zeros verified on critical line"
  seam := "Spectral gap → CCO bridge not formalized"
  verifier := "numerical"

/-- The Riemann certificate is NOT hard (heuristic). -/
theorem riemannSpectralCert_not_hard :
    riemannSpectralCert.isHard = false := by native_decide

/-- Therefore it cannot support a proven lock. -/
theorem riemannSpectralCert_no_proven :
    ¬ BealFoundry.permittedLock riemannSpectralCert.certType .proven := by
  simp [riemannSpectralCert, BealFoundry.permittedLock, BealFoundry.CertificateType.isHard]

end BealFoundry.Riemann